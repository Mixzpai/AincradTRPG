================================================================================
  AINCRAD TRPG — Project Documentation
  SAO-Themed Turn-Based ASCII Roguelike
================================================================================

  Engine:     C# / .NET 8.0 Console Application
  UI:         Terminal.Gui v2.0.0 (TUI framework)
  Platform:   Windows (with Win32 window sizing)
  Dependency: Terminal.Gui 2.0.0 (sole NuGet package)

================================================================================
  TABLE OF CONTENTS
================================================================================

  1. Overview
  2. Folder Structure
  3. Architecture & Design Patterns
  4. File Reference (every .cs file)
  5. Core Systems Breakdown
  6. UI Framework Notes (Terminal.Gui v2)
  7. Data Flow & Event Wiring
  8. Expandability Guide
  9. Build & Run

================================================================================
  1. OVERVIEW
================================================================================

  AincradTRPG is a Sword Art Online-themed turn-based ASCII roguelike.
  The player creates a character, allocates stats, and ascends through
  procedurally generated floors in the floating castle Aincrad (Floor 1 → 100).

  Core gameplay loop:
    Title Screen -> Difficulty Select -> Character Creation -> Gameplay
    Gameplay: Explore -> Fight Mobs -> Loot -> Level Up -> Ascend Stairs
    Death: Run Summary -> Return to Title

Core Gameplay:
    - Procedural dungeon generation with rooms, corridors, and clearings
    - Fog of war with visibility radius and light falloff
    - Per-mob aggro ranges and simple AI (chase, flee, wander)
    - Inventory system with equipment, consumables, and materials
    - Shop system with floor-scaling vendor inventories
    - Traps (spike, teleport), campfire healing, and stairs confirmation
    - 5 floor tiers with unique mob sets and scaling difficulty
    - 8 difficulty levels (Story through Unwinnable) scaling mob stats, XP, regen
    - Hardcore permadeath toggle (death = quit, no retry)
    - Auto-explore with step delay (X key)
    - Rest command (R key) — skip 3 turns, heal 3 HP/turn, blocked near enemies
    - Boss gate — stairs sealed until floor boss is defeated

  Combat & Status Effects:
    - Turn-based combat with crits, dodges, and XP diminishing returns
    - Monster critical hits (mobs roll crit chance using CriticalRate/CriticalHitDamage)
    - Kill streak callouts (Double Kill, Triple Kill, Rampage, etc.)
    - Kill-based title progression (Adventurer -> Blooded -> ... -> Legend)
    - Dodge streak callouts (Matrix mode at 3, Untouchable at 5, Phantom at 7+)
    - Poison status effect (Nepenthes, Scavenger Toad, Cursed Blade)
    - Bleed status effect (Kobold Warrior, Killer Mantis, Undead Knight, Dragon Knight)
    - Antidote item — cures poison and bleed (vendor stock + 40% drop from poison mobs)
    - Trap proximity sense (Dexterity-based chance to detect adjacent traps)
    - Monster threat level in Look/tile info (Deadly/Dangerous/Strong/Even/Weaker/Trivial)
    - Near-death mob indicators ("One more hit!" at <=10% HP)

  UI & HUD:
    - Color-coded game log with keyword-based highlighting
    - Minimap with current floor + exploration % (F1 42%)
    - HP/XP bars and stat panels
    - Low HP warning — action bar turns red below 25% health
    - Col currency display (¢) in the action bar HUD
    - Inventory button shows live item count
    - Equipped weapon name shown in HUD ([Iron Sword] or [Fists])
    - Live monster count on current floor (Mobs:5)
    - HUD status indicators: [POISON:N], [BLEED:N]
    - Stairs tile info shows [SEALED] or [OPEN] based on boss status
    - Death screen shows killer name, run rating (S/A/B/C/D grade)
    - "Press Enter to continue" hint on death screen
    - Max 1 aggro alert per turn to prevent log spam
    - Health-based player '@' color (yellow→red→pulsing at critical HP)
    - Damage numbers flash on map (floating combat numbers)
    - Corpse markers ('†') fade at death locations (red→gray color fade)
    - Loot sparkle ('!' flash) on ground items
    - Auto-save indicator ("[Saved]") on floor descent

  Map Visual Effects:
    - Fog of war edge gradient (soft boundary + ░▒▓ density shading)
    - Ambient particles (wind dots on grass, mist on water, campfire embers)
    - Door open/closed visual state ('▌' closed, '╌' opened, affects LOS)
    - Terrain transition borders (',' glyphs at water/lava edges)
    - Wall shadow casting (1-tile DarkGray shadow below walls)
    - Shrine aura glow (Magenta tint within 2 tiles of shrines)
    - Water edge foam (BrightCyan shoreline on land-adjacent water)
    - Water lily pads ('●' green on ~10% of shallow water tiles)
    - Room lighting variation (per-region color tints on floor tiles)
    - Explored room glow (warm Yellow tint on well-explored floors)
    - Depth-based tinting (progressive color dimming at medium range)
    - Wall edge shading (bright walls facing open floor, dim interior walls)
    - Cobweb decorations (gray '%' on wall corner tiles)
    - Lava danger zone (DarkGray bg within 2 tiles of lava)
    - SAO shatter particles ('◇' diamond burst on monster death)
    - Kill streak flash (magenta '@' + '★' burst on multi-kills)
    - Interactable sparkle (✦/✧ pulse on doors, stairs, shrines)

  Flavor Text & Atmosphere:
    - SAO-themed floor descent banners with per-floor flavor text
    - Time-of-day atmosphere on floor descent (real clock: dawn/morning/noon/etc.)
    - Random gameplay tips on the title screen
    - Personalized welcome messages with player name
    - Idle atmosphere flavor text after standing still 10+ turns
    - Environmental ambient messages (~4% chance per move)
    - Ambient sound text (~1% per turn near water/campfire/wind)
    - Turn milestone callouts every 100 turns
    - Floor completion message at 100% explored
    - Floor cleared message when all monsters defeated
    - Floor entry mob count announcement
    - Staircase discovery announcement (one-time when first spotted)

  Flavor Text — Combat:
    - Varied player attack flavor (5 lines)
    - Varied critical hit flavor (5 lines: Devastating, Perfect Strike, etc.)
    - Varied combat dodge/miss flavor (6 lines)
    - Varied combat strong block flavor (5 lines when defense absorbs 50%+)
    - Overkill flavor variety (Obliterated, Annihilated, Destroyed, etc.)
    - Monster defeat flavor variety (5 lines: "crumbles to dust", "shatters into light")
    - Monster aggro alerts when enemies first spot the player
    - Monster flee alerts when enemies start running at low HP
    - Low HP survival encouragement (~40% chance when hit below 25%)
    - First kill celebration message

  Flavor Text — Items & Interaction:
    - Rare/Epic loot drop fanfare with star prefix and glow descriptions
    - Item pickup flavor variety (4 lines)
    - Healing potion use flavor (5 atmospheric lines)
    - Food consumption flavor (5 atmospheric lines)
    - Equipment equip flavor variety (4 lines with item/slot name)
    - Inventory full warning variety (4 messages)
    - Empty ground pickup flavor (4 lines when pressing G with nothing underfoot)
    - Vendor greeting variety (5 random lines with shop name)
    - Vendor farewell on shop close (5 goodbye lines)
    - NPC fallback dialogue variety (6 random lines)

  Flavor Text — Movement & Environment:
    - Terrain-aware footstep flavor (water, grass, flowers, mountain, rock — 12% chance)
    - Campfire rest quotes — random SAO-flavored lines on heal
    - Full-health campfire message variety (5 lines)
    - Door passage flavor (4 lines, 12% chance)
    - Wall bump flavor (10% chance for witty one-liner)
    - Spike trap flavor variety (5 lines with damage amount)
    - Teleport trap landing flavor (5 disorientation lines)
    - Auto-explore stop flavor variety (enemy, items, done — 4 lines each)

  Flavor Text — Progression:
    - Level up fanfare with full HP restore and decorated log banner
    - Level-up motivational flavor (5 lines inside level-up banner)
    - Passive regen notification flavor (30% chance per tick, 5 lines)
    - Boss defeat fanfare with decorated log banner and stairs-open announcement
    - Player death message variety (5 dramatic lines)

================================================================================
  2. FOLDER STRUCTURE
================================================================================

  SAOTRPG/
  |
  |-- Program.cs                          Entry point, Win32 window setup
  |-- SAOTRPG.csproj                      Project file (.NET 8, Terminal.Gui)
  |-- SAOTRPG.sln                         Solution file
  |-- DOCUMENTATION.txt                   This file
  |-- debug.log                           Runtime debug output
  |
  |-- Entities/                           Game entities
  |   |-- Entity.cs                       Abstract base (position, stats, visuals)
  |   |-- IStatModifiable.cs              Interface for stat modification
  |   |-- Player.cs                       Player character (inventory, combat, XP)
  |   |-- Monster.cs                      Abstract monster (XP/col yields)
  |   |-- Boss.cs                         Boss with phase system
  |   |-- Mob.cs                          Standard mob (aggro range, appearance)
  |   |-- NPC.cs                          Abstract NPC (dialogue, interaction)
  |   |-- PC.cs                           Recruitable NPC
  |   |-- Vendor.cs                       Shop NPC (floor-scaled inventory)
  |   |-- WorldSpawn.cs                   Background decoration NPC
  |   |-- Bosses/
  |       |-- IllfangTheKobaldLord.cs     Floor 1 boss (multi-phase)
  |
  |-- Inventory/                          Inventory management system
  |   |-- Core/
  |   |   |-- Inventory.cs                Add/remove, equip/unequip, use, capacity
  |   |   |-- EquipmentSlot.cs            9 equipment slots enum
  |   |-- Equipment/
  |   |   |-- IEquipmentSlotResolver.cs   Interface for slot resolution
  |   |   |-- EquipmentSlotResolver.cs    Default string-to-slot mappings
  |   |-- Events/
  |   |   |-- InventoryEventArgs.cs       Event args (Item, Equipment, Consumable)
  |   |   |-- InventoryEvents.cs          Event hub for inventory changes
  |   |-- Logging/
  |   |   |-- IInventoryLogger.cs         Logging interface
  |   |   |-- ConsoleInventoryLogger.cs   Console output logger
  |   |-- Documentation/
  |       |-- Inventory.txt               Detailed design document
  |
  |-- Items/                              Item hierarchy
  |   |-- BaseItem.cs                     Abstract base (name, value, rarity)
  |   |-- abstract_BaseItem.cs            Legacy abstract base
  |   |-- StackableItem.cs               Stackable items with stacking logic
  |   |-- StatType.cs                     Stat enum (Health, Attack, Defense, etc.)
  |   |-- StatEffect.cs                   Single stat modification definition
  |   |-- StatModifierCollection.cs       Fluent API effect collection
  |   |
  |   |-- Equipment/
  |   |   |-- Equipment.cs                Abstract equipment (level req, bonuses)
  |   |   |-- EquipmentBase.cs            Alt base supporting IStatModifiable
  |   |   |-- Weapon.cs                   Attack power, damage type, speed, range
  |   |   |-- Armor.cs                    Defense, slot, weight
  |   |   |-- Accessory.cs               Accessory slot bonuses
  |   |
  |   |-- Consumables/
  |   |   |-- Consumable.cs               Abstract consumable with effects
  |   |   |-- Potion.cs                   Healing/buff with cooldowns
  |   |   |-- Food.cs                     Regen over time
  |   |   |-- DamageItem.cs              Throwable AoE damage
  |   |
  |   |-- Materials/
  |   |   |-- Material.cs                 Abstract crafting material
  |   |   |-- Gathered.cs                 Gathering drops (location, level req)
  |   |   |-- MobDrop.cs                  Monster drops (drop rate, boss flag)
  |   |
  |   |-- Definitions/                    Item data definitions (static catalogs)
  |   |   |-- AccessoryDefinitions.cs
  |   |   |-- ArmorDefinitions.cs
  |   |   |-- DamageItemDefinitions.cs
  |   |   |-- FoodDefinitions.cs
  |   |   |-- GatheredDefinitions.cs
  |   |   |-- MobDropDefinitions.cs
  |   |   |-- PotionDefinitions.cs
  |   |   |-- Weapons/
  |   |       |-- WeaponDefinitions.cs    Base weapon defs
  |   |       |-- AxeDefinitions.cs
  |   |       |-- BowDefinitions.cs
  |   |       |-- DaggerDefinitions.cs
  |   |       |-- KatanaDefinitions.cs
  |   |       |-- MaceDefinitions.cs
  |   |       |-- OneHandedSwordDefinitions.cs
  |   |       |-- RapierDefinitions.cs
  |   |       |-- ShieldDefinitions.cs
  |   |       |-- SpearDefinitions.cs
  |   |       |-- StaffDefinitions.cs
  |   |       |-- TwoHandedSwordDefinitions.cs
  |   |
  |   |-- Documentation/
  |       |-- DESIGN_CHOICES.txt          Items system architecture doc
  |
  |-- Map/                                Dungeon generation & management
  |   |-- Tile.cs                         Single tile (type, occupant, items)
  |   |-- TileType.cs                     30+ tile types enum (grass, wall, etc.)
  |   |-- Room.cs                         Room struct for generation
  |   |-- GameMap.cs                      Tile grid, fog of war, visibility
  |   |-- Camera.cs                       Viewport camera (centering, clamping)
  |   |-- MapGenerator.cs                 Procedural floor generator
  |   |-- MobFactory.cs                   Floor-tiered mob spawning
  |
  |-- Systems/                            Core game logic
  |   |-- TurnManager.cs                  Turn loop, combat, traps, stairs, XP
  |   |-- SimpleAI.cs                     Mob AI (aggro, chase, flee, wander)
  |
  |-- UI/                                 Terminal.Gui screens & components
      |-- TitleScreen.cs                  Main menu with ASCII art
      |-- DifficultyScreen.cs             Difficulty + hardcore selection
      |-- CharacterCreationScreen.cs      Identity input + stat allocation
      |-- GameScreen.cs                   Main gameplay (wires all components)
      |-- DeathScreen.cs                  Game over + run summary
      |-- OptionsScreen.cs               Placeholder options
      |-- MapView.cs                      Dungeon renderer (custom View)
      |-- MinimapView.cs                  Scaled floor overview (custom View)
      |-- ColoredLogView.cs              Keyword-colored log (custom View)
      |-- GameLogView.cs                  Log adapter (IGameLog -> ColoredLogView)
      |-- IGameLog.cs                     Log output interface
      |-- StringGameLog.cs               StringBuilder log sink
      |-- TerminalGuiInventoryLogger.cs   Inventory -> game log bridge
      |-- NavigationHelper.cs             W/S key navigation helper
      |-- DebugLogger.cs                  File-based debug logger
      |-- DebugMode.cs                    --debug flag detection
      |
      |-- Dialogs/                        Extracted modal dialogs
      |   |-- HelpDialog.cs               Keybindings + map legend (H key)
      |   |-- InventoryDialog.cs          Inventory management (I key)
      |   |-- StatsDialog.cs              Stat allocation (P key)
      |   |-- ShopDialog.cs               Buy/sell vendor interface
      |   |-- KillStatsDialog.cs          Run statistics (K key)
      |
      |-- Helpers/                        Shared UI utilities
          |-- BarBuilder.cs               Gradient bar renderer (HP/XP/SAT)
          |-- BoxDrawing.cs               Unicode box-drawing character library
          |-- ColorSchemes.cs             Centralized color palette
          |-- MapEffects.cs              Static map rendering post-effects
          |-- StatusTagBuilder.cs         Compact status effect tag builder

================================================================================
  3. ARCHITECTURE & DESIGN PATTERNS
================================================================================

  EVENT-DRIVEN ARCHITECTURE
  -------------------------
  The game uses events to decouple systems. MapView fires input events,
  GameScreen wires them to TurnManager, which fires state-change events
  back to the UI. Inventory raises events through InventoryEvents, which
  TerminalGuiInventoryLogger translates into log messages.

    MapView.PlayerMoveRequested -> GameScreen -> TurnManager.ProcessPlayerMove
    MapView.InventoryRequested -> GameScreen -> InventoryDialog.Show()
    TurnManager.TurnCompleted  -> GameScreen -> RefreshHud()
    TurnManager.StairsConfirmRequested -> GameScreen -> ShowStairsDialog

  INTERFACE ABSTRACTIONS
  ----------------------
  IGameLog           Two implementations: GameLogView (live UI), StringGameLog
                     (character creation). Systems write to IGameLog without
                     knowing which display is active.

  IInventoryLogger   Decouples inventory events from display. Console logger
                     for testing, TerminalGuiInventoryLogger for gameplay.

  IEquipmentSlotResolver   Maps equipment types to slots. Default resolver
                           included, swappable for custom slot rules.

  IStatModifiable    Entities that can have stats modified by items.

  DATA-DRIVEN DESIGN
  ------------------
  MobFactory         Uses MobTemplate records: add a mob by adding one line.
  ColoredLogView     Keyword-color rules as a static array of tuples.
  StatsDialog        StatDef records: add a stat by adding one tuple.
  DifficultyScreen   Parallel arrays of names/descriptions, auto-indexed.
  Item Definitions   Static catalogs in Definitions/ folder.

  FACTORY PATTERN
  ---------------
  MobFactory.CreateMobsForFloor()   Spawns floor-appropriate mobs from templates.
  Vendor                            Generates inventory based on floor number.
  MapGenerator                      Procedural floor generation with rooms/corridors.

  CENTRALIZED THEMING
  -------------------
  ColorSchemes.cs defines 8 shared palettes (Button, Gold, Dim, Body, Title,
  Danger, MenuFrame, MenuButton). Every UI file references these instead of
  creating inline ColorScheme objects. Changing a color in one place updates
  the entire game.

  EXTRACTED HELPERS
  -----------------
  BarBuilder.BuildGradient() Gradient bar with █▓▒░· sub-cell precision.
  NavigationHelper          W/S key menu navigation, shared across screens.
  CreateMenuButton()        Factory in TitleScreen for consistent menu items.
  CreateField()             Factory in CharacterCreationScreen for form fields.
  StatRow()                 Factory in DeathScreen for summary card rows.

================================================================================
  4. FILE REFERENCE
================================================================================

  74 source files organized into 6 top-level modules:

  ENTITIES (11 files)
  -------------------
  Entity.cs               Abstract base for all entities. Position (X, Y),
                          stats (HP, Attack, Defense), level, symbol, color.
                          Provides TakeDamage(), Heal(), IsDefeated.

  IStatModifiable.cs      Interface: ModifyStat(StatType, int).

  Player.cs               The player character. Manages inventory, equipment,
                          combat (Attack with crit chance), XP/leveling,
                          stat points, col currency. CreateNewPlayer() factory.
                          SpendSkillPoints() for allocation.

  Monster.cs              Abstract monster. XPValue, ColValue for rewards.
                          AttackPlayer() method for combat.

  Boss.cs                 Extends Monster with phase system, boss title,
                          and phase-specific stat changes.

  Mob.cs                  Standard mob entity. AggroRange (int, default 6),
                          SetAppearance(char, Color) for visual customization.
                          Used by MobFactory for all non-boss enemies.

  NPC.cs                  Abstract NPC with Name, Dialogue, Interact().

  PC.cs                   Player-recruitable NPC. IsRecruited flag.

  Vendor.cs               Shop NPC. GenerateInventory(floor) creates
                          floor-appropriate items for buying.

  WorldSpawn.cs           Non-interactive background NPC for atmosphere.

  IllfangTheKobaldLord.cs Floor 1 boss. Multi-phase with stat scaling.

  ITEMS (26 files)
  ----------------
  BaseItem.cs             Abstract: Name, Value, Rarity, Description.
  StackableItem.cs        Abstract: MaxStack, CurrentStack, CanStack().
  StatType.cs             Enum: Health, Attack, Defense, Vitality, Strength,
                          Endurance, Dexterity, Agility, Intelligence.
  StatEffect.cs           Record: StatType, Potency, Duration, IsPercentage.
  StatModifierCollection  List<StatEffect> with fluent .Add() chaining.

  Equipment/Weapon.cs     Damage, WeaponType, AttackSpeed, Range.
  Equipment/Armor.cs      Defense, ArmorSlot, Weight.
  Equipment/Accessory.cs  Various stat bonuses in accessory slots.
  Equipment/EquipmentBase Base with stat modifier support.

  Consumables/Potion.cs   HealAmount, BuffType, Cooldown.
  Consumables/Food.cs     RegenAmount, Duration, RegenPerTick.
  Consumables/DamageItem  Damage, AoERadius, ElementType.

  Materials/Gathered.cs   GatherLocation, LevelRequired.
  Materials/MobDrop.cs    DropRate (float), IsBossDrop.

  Definitions/ (13 files) Static item catalogs. Each file contains factory
                          methods returning pre-configured items. Weapon defs
                          split by type (Axe, Bow, Dagger, Katana, etc.)

  INVENTORY (6 files)
  -------------------
  Core/Inventory.cs       Central manager. AddItem(), RemoveItem(),
                          EquipItem(), UnequipItem(), UseConsumable().
                          Capacity tracking, equipped items dictionary.

  Core/EquipmentSlot.cs   Enum: Weapon, Head, Chest, Legs, Feet,
                          RightRing, LeftRing, Bracelet, Necklace.

  Events/InventoryEvents  Event hub: ItemAdded, ItemRemoved, ItemEquipped,
                          ItemUnequipped, ConsumableUsed.

  Logging/IInventoryLogger  Interface: LogItemAdded, LogItemEquipped, etc.

  MAP (7 files)
  -------------
  TileType.cs             30+ tile types: Floor, Wall, Grass, GrassTall,
                          GrassSparse, Flowers, Tree, TreePine, Bush, Water,
                          WaterDeep, Mountain, Rock, Path, Door, StairsUp,
                          StairsUp, TrapSpike, TrapTeleport, Campfire, etc.

  Tile.cs                 Single tile: Type, Occupant (Entity), Items list,
                          IsWalkable, IsExplored, IsVisible, HasItems.

  Room.cs                 Rectangle struct with Intersects() for generation.

  GameMap.cs              2D tile grid. InBounds(), GetTile(), IsExplored(),
                          IsVisible(), UpdateVisibility() with radius-based
                          fog of war.

  Camera.cs               Viewport offset. CenterOn(x, y, mapW, mapH).
                          Clamps to prevent showing out-of-bounds tiles.

  MapGenerator.cs         Procedural generation:
                          - Room placement with overlap rejection
                          - L-shaped corridor connections
                          - Clearings with campfires (67% chance)
                          - Trap placement (4 + floor*2 + rand, 8-tile spawn buffer)
                          - Mob spawning via MobFactory
                          - Stairs, vendors, items

  MobFactory.cs           5 floor tiers, each with 5 mob templates:
                          Floors 1-5:   Kobold Scout, Forest Wolf, etc.
                          Floors 6-10:  Iron Golem, Shadow Lurker, etc.
                          Floors 11-20: Flame Sprite, Sand Worm, etc.
                          Floors 21-50: Frost Wraith, Storm Eagle, etc.
                          Floors 51+:   Void Walker, Abyssal Serpent, etc.
                          Uses record MobTemplate(Name, Symbol, Color, Aggro).

  SYSTEMS (2 files)
  -----------------
  TurnManager.cs          Core turn loop. Processes player actions, then
                          runs AI for all mobs. Handles:
                          - Movement with collision detection
                          - Combat (attack, crit, dodge, damage)
                          - XP with diminishing returns (halved at 3+ level diff)
                          - Loot drops on kill
                          - Kill streak tracking (Double/Triple/Quad/Rampage/Unstoppable)
                          - Kill-based title progression (9 tiers, Adventurer to Legend)
                          - Near-death mob indicators (dramatic callouts at <=25% HP)
                          - Trap effects (spike damage, teleport)
                          - Campfire healing (15 + floor * 5 HP) with rest quotes
                          - Terrain footstep flavor (water, grass, flowers, etc.)
                          - Turn milestone callouts every 100 turns
                          - Stairs confirmation dialog
                          - Floor transition with SAO-themed flavor text (floors 2-10+)
                          - Idle flavor text (atmospheric lines after 10+ still turns)
                          Events: TurnCompleted, DamageDealt, MonsterKilled,
                          FloorChanged, PlayerDied, GameWon,
                          VendorInteraction, StairsConfirmRequested

  SimpleAI.cs             Per-mob AI using Chebyshev distance:
                          - Idle: wander randomly
                          - Aggro: chase player if within AggroRange
                          - Flee: retreat when low HP (< 20%)
                          - Attack: deal damage when adjacent
                          Variable aggro per mob (scouts 8, golems 3, etc.)

  UI (18 files + 6 in subfolders)
  --------------------------------
  TitleScreen.cs          ASCII art banner ("AINCRAD" / "TRPG"), menu buttons,
                          animated star field, random gameplay tips in footer.
                          CreateMenuButton() / CreateMenuSeparator() factories.
                          Tips[] array — add new tips by adding a string.

  DifficultyScreen.cs     8 difficulty tiers + debug mode. RadioGroup selector,
                          dynamic description. Hardcore toggle with flavor
                          popups for wild combos (Story+Hardcore, etc.)
                          W/S wrapping navigation wired across all controls.

  CharacterCreationScreen Phase 1: First/Last/Gender fields with validation.
                          Phase 2: Stat allocation with live preview.
                          CreateField() factory for form field pairs.

  GameScreen.cs           Main gameplay compositor (~570 lines).
                          Wires: MapView, MinimapView, ColoredLogView, HUD
                          labels, action bar. Handles dialog opens, HUD
                          refresh, auto-explore timer, event subscriptions.
                          Low HP warning: bar turns red when HP <= 25%.
                          Named constants: AnimationIntervalMs, AutoExploreStepMs.

  DeathScreen.cs          "YOU DIED" banner, player identity line, boxed run
                          summary (floor, kills, turns, col, items, level).
                          StatRow() factory for easy summary expansion.

  MapView.cs              Custom View with OnDrawingContent() override.
                          Per-character colored rendering via Driver.SetAttribute
                          + Driver.AddRune. ResolveTileVisual() extracts tile
                          appearance. GetDimColor() for explored-but-dark tiles.
                          Light sources (torches, campfires) with glow radius.
                          Named constants: VisibilityRadius=18, FalloffThreshold,
                          LightGlowRadius=2.

  MinimapView.cs          Custom View. Auto-scales full map into viewport.
                          3-pass rendering: exploration state -> entities -> terrain.
                          Switch-expression lookup tables for terrain glyphs/colors.

  ColoredLogView.cs       Custom View. Data-driven keyword color rules:
                          "CRITICAL HIT" -> BrightMagenta, "defeated!" -> BrightYellow,
                          etc. Category fallback dictionary. Scrolling with
                          Up/Down/PageUp/PageDown, auto-scroll on new entries.

  GameLogView.cs          IGameLog adapter: routes Log/LogCombat/LogSystem
                          to ColoredLogView with prefix tags ([!], [*]).

  IGameLog.cs             Interface: Log(), LogCombat(), LogSystem().
                          Expandability note: add LogLoot() etc. by adding
                          to interface + both implementations.

  StringGameLog.cs        StringBuilder sink for character creation phase.

  TerminalGuiInventoryLogger  Bridges IInventoryLogger to IGameLog. Translates
                          inventory events into readable log messages.

  NavigationHelper.cs     EnableGameNavigation(): W/S/Arrow focus cycling.
                          RadioGroup-aware (lets arrows control selection).
                          ButtonScheme delegates to ColorSchemes.Button.

  DebugLogger.cs          Writes to debug.log. Categories: SESSION, INPUT,
                          SCREEN, GAME, COMBAT, SYSTEM, PERF, ERROR.
                          StartTimer/EndTimer for performance profiling.

  DebugMode.cs            Static IsEnabled flag. Set by --debug CLI arg.

  Dialogs/HelpDialog.cs   Full keybinding reference: Movement, Actions,
                          Interaction, Map Legend. Opens with H key.

  Dialogs/InventoryDialog  Item list with rarity tags ([E]pic, [R]are, [U]ncommon).
                          Use, Equip, Drop actions. RefreshAfterChange() DRY helper.

  Dialogs/StatsDialog.cs  StatDef record array. Add stats by adding a tuple.
                          Live stat preview with effects column.

  Dialogs/ShopDialog.cs   Buy/sell mode toggle. CloneShopItem() for safe purchase.
                          CalcSellPrice() and FormatItemInfo() helpers.

  Helpers/BarBuilder.cs   BuildGradient(current, max, width) with █▓▒░· sub-cell.
                          Build(), BuildHp(), BuildXp() convenience wrappers.

  Helpers/ColorSchemes.cs 8 centralized palettes: Button, Gold, Dim, Body,
                          Title, Danger, MenuFrame, MenuButton. Every UI
                          file references these. Change once, update everywhere.

  Helpers/MapEffects.cs   Static helpers for map rendering post-effects.
                          Extracted from MapView for readability. 20+ methods:
                          GetFogEdgeColor() — fog edge gradient near unexplored
                          GetFogDensityGlyph() — ░▒▓ fog density shading
                          GetAmbientParticle() — wind/mist/ember particles
                          GetTransitionBorder() — biome transition glyphs
                          IsInWallShadow() — 1-tile shadow below walls
                          GetShrineDistance() — mystic glow radius check
                          IsWaterEdge() — shoreline foam detection
                          GetRoomTint() — per-region floor color variation
                          GetDepthTintedColor() — distance-based color dimming
                          GetRainParticle() — weather rain overlay
                          GetWeatherDimmedColor() — rain desaturation
                          GetWaterFlowGlyph() — animated wave propagation
                          GetCampfireFlickerRadius() — pulsing campfire light
                          GetCampfireGlowDistance() — campfire glow check
                          ShouldShowAggroIndicator() — monster chase indicator
                          GetInteractableSparkle() — ✦/✧ on interactables
                          GetHeatmapColor() — explored area heat map
                          GetWaterLilyPad() — static lily pad decoration
                          IsInLavaDangerZone() — lava proximity check
                          GetCobwebDecoration() — wall corner cobwebs
                          GetExploredRoomGlow() — warm tint on explored rooms
                          GetAmbientSoundText() — flavor text near terrain

================================================================================
  5. CORE SYSTEMS BREAKDOWN
================================================================================

  TURN LOOP
  ---------
  1. Player presses a movement/action key in MapView
  2. MapView fires an event (MoveRequested, InteractRequested, etc.)
  3. GameScreen routes the event to TurnManager
  4. TurnManager processes the player action:
     - Move: collision check, tile effects (traps, campfires, stairs)
     - Attack: damage calc with crit/dodge rolls
     - Interact: NPC dialogue, vendor shop
  5. TurnManager runs SimpleAI for each living mob
  6. TurnManager fires HudRefresh event
  7. GameScreen updates all HUD labels and redraws

  COMBAT
  ------
  Player attacks:
    damage = Attack - monster.Defense (minimum 1)
    crit chance based on Dexterity
    crit multiplier: 2x damage
  Monster attacks:
    damage = monster.Attack - player.Defense (minimum 1)
    dodge chance based on player Agility
  On kill:
    XP awarded with diminishing returns (halved at 3+ lvl diff, quartered at 5+)
    Col dropped
    Item drop chance

  MAP GENERATION
  --------------
  1. Fill grid with walls
  2. Place rooms (random size/position, reject overlaps)
  3. Connect rooms with L-shaped corridors
  4. Add clearings (open grassy areas with campfires)
  5. Place terrain features (trees, water, rocks, flowers)
  6. Place stairs up (farthest room from spawn)
  7. Spawn mobs via MobFactory (floor-tiered templates)
  8. Place traps (spike, teleport) away from spawn
  9. Place vendors and items

  VISIBILITY
  ----------
  - Radius-based fog of war (VisibilityRadius = 18)
  - Light falloff: tiles beyond FalloffThreshold (75%) are dimmed
  - Light sources (torches, campfires) have LightGlowRadius = 2
  - Explored tiles stay visible but dimmed (DarkGray)
  - Unexplored tiles are completely black

  MOB AI
  ------
  Per-tick decision for each mob:
  1. Calculate Chebyshev distance to player
  2. If distance <= mob.AggroRange: chase (pathfind toward player)
  3. If adjacent to player: attack
  4. If HP < 20% of max: flee (move away from player)
  5. Otherwise: wander randomly

================================================================================
  6. UI FRAMEWORK NOTES (Terminal.Gui v2)
================================================================================

  Terminal.Gui v2 key concepts used in this project:

  CUSTOM VIEWS
  - Subclass View and override OnDrawingContent()
  - Driver.SetAttribute(new Terminal.Gui.Attribute(fg, bg)) for colors
  - Driver.AddRune(new System.Text.Rune(ch)) for characters
  - Move(x, y) to position cursor within the view
  - SetNeedsDraw() to trigger redraw

  LAYOUT SYSTEM
  - Pos: Center(), Percent(), Bottom(view), AnchorEnd()
  - Dim: Fill(), Auto(), Percent()
  - Views are positioned relative to parent or sibling views

  MODAL DIALOGS
  - Application.Run(dialog) blocks until RequestStop()
  - Dialog class provides built-in button bar
  - MessageBox.Query() for simple confirmations

  TIMERS
  - Application.AddTimeout(TimeSpan, callback) for periodic updates
  - Returns token for cancellation
  - Used for animation cycling and auto-explore stepping

  COLOR SCHEMES
  - ColorScheme has 5 states: Normal, Focus, HotNormal, HotFocus, Disabled
  - Terminal.Gui.Attribute(Color fg, Color bg) — fully qualified to avoid
    System.Attribute ambiguity
  - All palettes centralized in ColorSchemes.cs

  INPUT HANDLING
  - KeyDown event on views
  - e.KeyCode for key identification
  - e.Handled = true to consume the event
  - Accepting event on Button for click handling
  - e.Cancel = true in Accepting to prevent default behavior

  DATA BINDING
  - ObservableCollection<T> with ListWrapper<T> for ListView
  - Manual refresh pattern: update data, call SetNeedsDraw()

================================================================================
  7. DATA FLOW & EVENT WIRING
================================================================================

  GAME STARTUP FLOW
  -----------------
  Program.Main()
    -> Application.Init()
    -> Window (fullscreen, borderless)
    -> TitleScreen.Show(mainWindow)

  SCREEN TRANSITIONS
  ------------------
  TitleScreen       -> DifficultyScreen.Show()     [New Game button]
  TitleScreen       -> OptionsScreen.Show()        [Options button]
  DifficultyScreen  -> CharacterCreationScreen     [Continue button]
  DifficultyScreen  -> TitleScreen                 [Back button]
  CharacterCreation -> GameScreen.Show(player)     [Start Game button]
  GameScreen        -> DeathScreen                 [Player HP <= 0]
  DeathScreen       -> TitleScreen                 [Return button]

  GAMEPLAY EVENT CHAIN
  --------------------
  MapView events:
    PlayerMoveRequested(dx,dy) -> TurnManager.ProcessPlayerMove()
    SprintRequested(dx,dy)     -> TurnManager.ProcessSprint()
    StealthMoveRequested(dx,dy)-> TurnManager.ProcessStealthMove()
    InventoryRequested         -> InventoryDialog.Show()
    StatsRequested             -> StatsDialog.Show()
    HelpRequested              -> HelpDialog.Show()
    PickupRequested            -> TurnManager.PickupItems()
    RestRequested              -> TurnManager.ProcessRest()
    AutoExploreRequested       -> Auto-explore timer loop
    WaitRequested              -> TurnManager.ProcessPlayerMove(0,0)
    LookRequested              -> Log surrounding tile info
    KillStatsRequested         -> KillStatsDialog.Show()

  TurnManager events:
    TurnCompleted              -> GameScreen refreshes HUD
    DamageDealt(x,y,dmg,bool)  -> MapView damage flash + scorch marks
    MonsterKilled(x,y)         -> MapView corpse/shatter + kill streak flash
    StairsConfirmRequested     -> GameScreen shows stairs dialog
    FloorChanged(floor)        -> Update map refs, title, weather
    PlayerDied                 -> DeathScreen.Show()
    GameWon                    -> VictoryScreen.Show()
    VendorInteraction(vendor)  -> ShopDialog.Show()

  Inventory events:
    ItemAdded/Removed         -> TerminalGuiInventoryLogger -> GameLogView
    ItemEquipped/Unequipped   -> TerminalGuiInventoryLogger -> GameLogView
    ConsumableUsed            -> TerminalGuiInventoryLogger -> GameLogView

================================================================================
  8. EXPANDABILITY GUIDE
================================================================================

  This section describes how to add common new features.

  ADDING A NEW MOB
  ----------------
  File: Map/MobFactory.cs
  1. Find the floor tier array (e.g. _floor1to5)
  2. Add a new MobTemplate record:
     new MobTemplate("Mob Name", 'x', Color.Green, 6)
     (name, symbol, color, aggro range)
  That's it. The factory randomly picks from the tier array.

  ADDING A NEW TILE TYPE
  ----------------------
  File: Map/TileType.cs
  1. Add entry to TileType enum
  File: UI/MapView.cs
  2. Add visual mapping in ResolveTileVisual() switch expression
  File: UI/MinimapView.cs
  3. Add glyph in GetTerrainGlyph() and color in GetTerrainColor()
  File: Map/MapGenerator.cs
  4. Add placement logic if it should spawn naturally

  ADDING A NEW LOG COLOR RULE
  ---------------------------
  File: UI/ColoredLogView.cs
  1. Add a tuple to the KeywordRules array:
     ("keyword", Color.SomeColor)
  Keywords are checked case-insensitively, first match wins.

  ADDING A NEW STAT TO THE DEATH SCREEN
  --------------------------------------
  File: UI/DeathScreen.cs
  1. Add a StatRow() line in BuildSummary():
     StatRow("New Stat", value)

  ADDING A NEW DIALOG
  -------------------
  1. Create UI/Dialogs/NewDialog.cs
  2. Static class with Show(Window, Player, ...) method
  3. Use ColorSchemes.Body/Title/Button for consistency
  4. Wire the open trigger in GameScreen (key event or button)

  ADDING A NEW LOG CATEGORY
  -------------------------
  File: UI/IGameLog.cs
  1. Add method (e.g. void LogLoot(string message))
  File: UI/GameLogView.cs
  2. Implement with prefix and LogCategory
  File: UI/StringGameLog.cs
  3. Implement (just _sb.AppendLine)
  File: UI/ColoredLogView.cs
  4. Add LogCategory enum entry and default color

  ADDING A NEW COLOR SCHEME
  -------------------------
  File: UI/Helpers/ColorSchemes.cs
  1. Add a new public static readonly ColorScheme field
  2. Define Normal, Focus, HotNormal, HotFocus, Disabled states
  3. Reference it anywhere as ColorSchemes.NewScheme

  ADDING A NEW EQUIPMENT SLOT
  ---------------------------
  File: Inventory/Core/EquipmentSlot.cs
  1. Add enum entry
  File: Inventory/Equipment/EquipmentSlotResolver.cs
  2. Add mapping in the resolver

  ADDING A NEW ITEM DEFINITION
  ----------------------------
  1. Find the appropriate Definitions/ file (or create one)
  2. Add a static factory method returning a configured item
  3. Reference it in MapGenerator or Vendor for spawning

  ADDING A NEW SCREEN
  -------------------
  1. Create UI/NewScreen.cs as a static class
  2. public static void Show(Window mainWindow) pattern
  3. mainWindow.RemoveAll() at the top
  4. Use ColorSchemes for all color definitions
  5. Add navigation from an existing screen

  ADDING A NEW BOSS
  -----------------
  File: Entities/Bosses/NewBoss.cs
  1. Extend Boss class
  2. Define phases with stat overrides
  3. Wire into MapGenerator for the appropriate floor

  ADDING A NEW PLAYER TITLE
  -------------------------
  File: Systems/TurnManager.cs
  1. Add a (Kills, Title) tuple to TitleThresholds array
     (sorted descending by kill count — first match wins)

  ADDING NEW TERRAIN FLAVOR TEXT
  ------------------------------
  File: Systems/TurnManager.cs
  1. Add a { TileType.X, new[] { "flavor1", "flavor2" } } entry
     to the TerrainFlavors dictionary
  2. FootstepChance (12%) controls how often flavor appears

  ADDING A NEW CAMPFIRE QUOTE
  ---------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the CampfireQuotes array

  ADDING A NEW TURN MILESTONE MESSAGE
  ------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to MilestoneMessages (use {0} for turn count)
  2. MilestoneInterval controls frequency (default: every 100 turns)

  ADDING A NEW AMBIENT ATMOSPHERE MESSAGE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the AmbientMessages array
  2. AmbientChance (4%) controls how often they appear per move

  ADDING A NEW DODGE FLAVOR LINE
  ------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the DodgeFlavors array (use {0} for monster name)

  ADDING A NEW FLOOR COMPLETION MESSAGE
  --------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the FloorCompleteMessages array

  ADDING A NEW FLOOR CLEARED MESSAGE
  -----------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the FloorClearedMessages array

  ADDING A NEW CRITICAL HIT FLAVOR LINE
  --------------------------------------
  File: Entities/Player.cs
  1. Add a string to the CritFlavors array (use {0}=player, {1}=monster, {2}=damage)

  ADDING A NEW MONSTER AGGRO ALERT
  ---------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the AggroAlerts array (use {0} for monster name)

  ADDING A NEW VENDOR GREETING
  ----------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the VendorGreetings array (use {0} for shop name)

  ADDING A NEW LOW HP ENCOURAGEMENT
  ----------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the LowHpEncouragements array

  ADDING A NEW STAIRCASE DISCOVERY MESSAGE
  -----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the StairsDiscoveryMessages array

  ADDING A NEW OVERKILL FLAVOR LINE
  ----------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the OverkillFlavors array (use {0} for bonus EXP)

  ADDING A NEW MONSTER FLEE ALERT
  --------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the FleeFlavors array (use {0} for monster name)

  ADDING A NEW INVENTORY FULL MESSAGE
  ------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the InventoryFullFlavors array

  ADDING A NEW NPC FALLBACK DIALOGUE LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the NpcFallbackDialogue array

  ADDING A NEW NORMAL ATTACK FLAVOR LINE
  ----------------------------------------
  File: Entities/Player.cs
  1. Add a string to the AttackFlavors array (use {0}=player, {1}=monster, {2}=damage)

  ADDING A NEW RARE LOOT FANFARE LINE
  -------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the RareLootFlavors array (use {0}=rarity, {1}=item name)

  ADDING A NEW TRAP SENSE FLAVOR LINE
  -------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the TrapSenseFlavors array

  ADDING A NEW POTION USE FLAVOR LINE
  ------------------------------------
  File: UI/TerminalGuiInventoryLogger.cs
  1. Add a string to the PotionFlavors array

  ADDING A NEW MONSTER DEFEAT FLAVOR LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the DefeatFlavors array (use {0} for monster name)

  ADDING A NEW ITEM PICKUP FLAVOR LINE
  --------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the PickupFlavors array (use {0} for item name)

  ADDING A NEW WALL BUMP FLAVOR LINE
  ------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the WallBumpFlavors array

  ADDING A NEW FOOD USE FLAVOR LINE
  -----------------------------------
  File: UI/TerminalGuiInventoryLogger.cs
  1. Add a string to the FoodFlavors array

  ADDING A NEW EQUIP FLAVOR LINE
  --------------------------------
  File: UI/TerminalGuiInventoryLogger.cs
  1. Add a string to the EquipFlavors array (use {0}=item, {1}=slot)

  ADDING A NEW PASSIVE REGEN FLAVOR LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the RegenFlavors array

  ADDING A NEW DEATH SCREEN RUN GRADE
  -------------------------------------
  File: UI/DeathScreen.cs
  1. Add a (MinScore, Grade, Comment) tuple to RunGrades array
     (sorted descending by min score — first match wins)

  ADDING A NEW AUTO-EXPLORE STOP FLAVOR LINE
  --------------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to AutoExploreEnemyFlavors (enemy interrupt)
  2. Add a string to AutoExploreItemFlavors (items found)
  3. Add a string to AutoExploreDoneFlavors (fully explored)

  ADDING A NEW TELEPORT LANDING FLAVOR LINE
  -------------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the TeleportLandingFlavors array

  ADDING A NEW DOOR PASSAGE FLAVOR LINE
  ---------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the Door entry in the TerrainFlavors dictionary

  ADDING A NEW CAMPFIRE FULL HP FLAVOR LINE
  -------------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the CampfireFullHpFlavors array

  ADDING A NEW FLOOR MOB COUNT ANNOUNCEMENT
  -------------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the FloorMobCountFlavors array (use {0} for mob count)

  ADDING A NEW PLAYER DEATH FLAVOR LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the DeathFlavors array

  ADDING A NEW STRONG BLOCK FLAVOR LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the BlockFlavors array

  ADDING A NEW SPIKE TRAP FLAVOR LINE
  -------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the SpikeTrapFlavors array (use {0} for damage amount)

  ADDING A NEW EMPTY GROUND FLAVOR LINE
  ----------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the EmptyGroundFlavors array

  ADDING A NEW VENDOR FAREWELL LINE
  -----------------------------------
  File: UI/GameScreen.cs
  1. Add a string to the farewells array in the VendorInteraction handler
     (use {0} for vendor name)

  ADDING A NEW LEVEL-UP FLAVOR LINE
  -----------------------------------
  File: Entities/Player.cs
  1. Add a string to the LevelUpFlavors array

  ADDING MOUNTAIN/ROCK TERRAIN FLAVOR
  -------------------------------------
  File: Systems/TurnManager.cs
  1. Add a string to the Mountain or Rock entry in the TerrainFlavors dictionary

  ADDING A NEW DIFFICULTY TIER
  ----------------------------
  File: Systems/TurnManager.cs
  1. Add an entry to each of: MobStatScale, XpScale, RegenInterval arrays
     (index must match the DifficultyScreen radio group order)
  File: UI/DifficultyScreen.cs
  2. Add the tier name + description in BuildDifficultyData()
  File: UI/GameScreen.cs
  3. Add the tier name to the diffNames array

  ADDING A NEW POISONOUS MOB
  ---------------------------
  File: Map/MobFactory.cs
  1. Set Poison: true in the MobTemplate constructor:
     new MobTemplate("Toxic Snake", 's', Color.Green, 5, Poison: true)

  ADDING A NEW STATUS EFFECT
  ---------------------------
  File: Systems/TurnManager.cs
  1. Add tracking fields (_effectTurnsLeft, _effectDamagePerTick)
  2. Add a TickEffect() method (see TickPoison() or TickBleed() for reference)
  3. Call TickEffect() from both turn advancement points and ProcessRest()
  4. Reset in AscendFloor()
  5. Expose IsFoo / FooTurnsLeft for HUD display
  6. Add to antidote handler if it should be curable

  ADDING A NEW BLEEDING MOB
  --------------------------
  File: Map/MobFactory.cs
  1. Set Bleed: true in the MobTemplate constructor:
     new MobTemplate("Sharp Claw", 'c', Color.Red, 5, Bleed: true)

  ADDING A NEW CONSUMABLE TO VENDOR
  ----------------------------------
  File: Entities/Vendor.cs
  1. Add a ShopStock.Add(...) line in GenerateStock()
  2. Optionally gate behind a floor check (e.g. if (floor >= 3))

================================================================================
  9. BUILD & RUN
================================================================================

  REQUIREMENTS
  - .NET 8.0 SDK
  - Windows (Win32 window sizing in Program.cs)

  BUILD
    cd SAOTRPG
    dotnet build

  RUN
    dotnet run

  RUN WITH DEBUG MODE
    dotnet run -- --debug
    (Enables debug difficulty tier, verbose logging to debug.log)

  DEBUG LOG
    The debug.log file in the project root captures:
    - Session start/end timestamps
    - Screen transitions
    - Input events
    - Combat calculations
    - Performance timers
    - Errors and exceptions

================================================================================
  BATCH 16 — GAMEPLAY MECHANICS
================================================================================

  1. FLOOR-SPECIFIC BOSSES (Map/BossFactory.cs)
     - Data-driven boss table: (Name, Title, Level, Atk, Def, Hp, Xp, Col) per floor
     - 5 unique bosses: Illfang (F1), Asterius (F2), Nerius (F3), Wythege (F4), Kagachi (F5)
     - GenericBoss subclass for factory-created bosses
     - MapGenerator now calls BossFactory.CreateFloorBoss(floor) instead of hardcoded boss
     - To add bosses: append to FloorBosses array in BossFactory.cs

  2. EQUIPMENT DURABILITY (Systems/TurnManager.cs, Inventory/Core/Inventory.cs)
     - Weapons lose 1 durability per player attack
     - Chest armor loses 1 durability per hit taken from monsters
     - At 0 durability: item is unequipped, stat bonuses removed, item destroyed
     - Warning at 5 durability remaining
     - HUD shows weapon durability: [Weapon Name (42)]
     - Inventory.DestroyEquipped(slot) — force-removes broken equipment
     - To add more degrading slots: call DegradeEquipment(slot) at appropriate points

  3. DANGER SENSE (Systems/TurnManager.cs)
     - When a monster 3+ levels above the player first aggros, a warning is logged
     - 3-5 levels above: "!! DANGER !!" prefix
     - 6+ levels above: "!!! EXTREME DANGER !!!" prefix
     - Once-per-monster via _dangerWarned HashSet (cleared on floor change)

  4. PERFECT KILL COL BONUS (Systems/TurnManager.cs)
     - One-shot kills (monster dies on first bump, player takes no damage) award +50% Col
     - Tracked by comparing player HP before and after the bump attack
     - Displayed in log: "PERFECT +X Col"

  5. CAMPFIRE STATUS CLEANSE (Systems/TurnManager.cs)
     - Stepping on a campfire now cures poison and bleed in addition to healing
     - Cleanse happens before the HP heal
     - Tile info updated: "A warm campfire (heals HP, cures status)"
     - If player has status but full HP, still shows cleanse message

================================================================================
  BATCH 17 — COMBAT & MOVEMENT SYSTEMS
================================================================================

  1. LOOT RARITY SCALING (Systems/TurnManager.cs)
     - Rarity now affects equipment stats, durability, and value
     - Common: base stats | Uncommon: +25% stats, +10 dur, 1.5x value
     - Rare: +60% stats, +25 dur, 2.5x value | Epic: 2x stats, +50 dur, 4x value
     - Data-driven via RarityScaling array + RarityIndex helper
     - To add rarity tiers: extend RarityScaling array and PickRarity() thresholds

  2. SPRINT MECHANIC (UI/MapView.cs, Systems/TurnManager.cs, UI/GameScreen.cs)
     - Shift+direction key = move 2 tiles in one turn
     - Both tiles must be walkable and unoccupied (can't sprint into monsters)
     - Skips tile effects (traps/campfire) — tradeoff for speed
     - SprintRequested event with (dx, dy) from MapView
     - Keybind: Shift+WASD or Shift+Arrows (also works with diagonals)

  3. MONSTER HP BARS IN LOG (Systems/TurnManager.cs)
     - Combat log now shows ASCII HP bar when hitting a monster
     - Format: "Kobold Grunt [####--------] 15/45 (wounded)"
     - BuildMobHpBar() generates [###---] style bar (12 chars wide)
     - Combined with existing condition text (healthy/wounded/badly hurt/near death)

  4. FLOOR TIMER BONUS (Systems/TurnManager.cs)
     - Par turn count per floor (200/220/250/280/320... scaling)
     - Clearing a floor under par awards bonus Col (50 + floor * 30)
     - Tracked via _floorStartTurn, checked in AscendFloor
     - To adjust pacing: modify FloorParTurns array

  5. WEAPON PROFICIENCY (Systems/TurnManager.cs)
     - Kills tracked per weapon type (One-Handed Sword, Dagger, Unarmed, etc.)
     - Milestone ranks with passive damage bonus:
       10 kills = Novice (+1) | 25 = Apprentice (+2) | 50 = Journeyman (+4)
       100 = Expert (+7) | 200 = Master (+11)
     - Rank-up announced in log with bonus shown
     - Bonus applied via GetProficiencyBonus() before AttackMonster
     - To add ranks: extend ProficiencyRanks array

================================================================================
  BATCH 18 — WORLD & COMBAT DEPTH
================================================================================

  1. TREASURE CHESTS (Map/MapGenerator.cs)
     - Loot scattered on tiles across the map (2 + floor + rand(3) per floor)
     - Each chest has 1-3 items: potions, food, antidotes, battle elixirs
     - Weighted drop table: 35% HP pot, 15% bread, 15% antidote, 15% meat, 12% greater pot, 8% elixir
     - Uses existing tile Items list + G pickup system
     - To add new chest loot: extend the loot switch in PopulateFloor

  2. COMBO ATTACKS (Systems/TurnManager.cs)
     - Consecutive hits on the same target deal escalating bonus damage
     - Formula: +0 on hit 1, +2 on hit 2, +4 on hit 3, etc. (+2 per consecutive hit)
     - Combo counter displayed at 3+ hits: "3-hit combo! (+4 bonus damage)"
     - Switching targets resets the combo
     - Tracked via _comboTarget (monster ID) and _comboCount

  3. MONSTER VARIANTS (Map/MobFactory.cs, Entities/Mob.cs)
     - 10% chance: Elite (yellow, 1.5x stats, 2x XP/Col, +2 levels)
     - 3% chance: Champion (magenta, uppercase symbol, 2x stats, 3x XP/Col, +3 levels)
     - Mob.Variant property: "", "Elite", or "Champion"
     - Champions use uppercase symbol for visual distinction
     - To add variants: extend the variantRoll block in CreateFloorMob

  4. HUNGER SYSTEM (Systems/TurnManager.cs)
     - Satiety: 0-100, drains 1 every 3 turns
     - Below 30: no passive HP regen, [HUNGRY:N] shown on HUD
     - At 0: take 1 damage/turn (starvation), can kill
     - Food restores satiety: Bread=20, Grilled Meat=30 (RegenerationDuration * 2)
     - Campfire fountains also restore +20 satiety
     - Warning at satiety 15: "Your stomach growls..."
     - To tune: adjust HungerDrainInterval, HungerRegenThreshold, MaxSatiety

  5. MAP LANDMARKS (Map/TileType.cs, Map/MapGenerator.cs, Systems/TurnManager.cs)
     - Fountain (O, animated cyan/blue): heals 30+floor*10 HP, +20 satiety, one use
     - Shrine ($, magenta): +3+floor ATK/DEF buff for 30 turns, one use
     - Pillar (|, cyan): reveals 15-tile radius of map, one use
     - All landmarks convert to Floor after use
     - 1 of each spawned per floor, placed away from spawn
     - HUD shows [BLESSED:N] when shrine buff active
     - GameMap.SetExplored() added for pillar reveal
     - To add landmarks: add TileType + visual + generator placement + TurnManager effect

================================================================================
  BATCH 19 — HAZARDS, STEALTH & ECONOMY
================================================================================

  1. MINI-BOSS ROOMS (Map/MapGenerator.cs)
     - A locked structure room spawns mid-map (if enough clearings exist)
     - Contains a Champion-tier mini-boss (2x stats, 3x XP/Col, +3 levels)
     - Guaranteed Rare or Epic equipment drop (weapon or armor) on the floor
     - Equipment scales with floor number (ATK/DEF, durability, value)
     - To add mini-boss variants: modify the upgrade block in PopulateFloor

  2. STEALTH MECHANIC (UI/MapView.cs, Systems/SimpleAI.cs, Systems/TurnManager.cs)
     - Ctrl+direction = stealth move (1 tile, halves monster aggro range)
     - SimpleAI.DecideAction accepts playerStealthed flag, divides aggro by 2
     - Log message: "You move silently..."
     - HUD shows [STEALTH] during stealth turns
     - StealthMoveRequested event from MapView → ProcessStealthMove in TurnManager
     - To tune: adjust the aggro divisor in SimpleAI

  3. LAVA TILES (Map/TileType.cs, Map/MapGenerator.cs, Systems/TurnManager.cs)
     - New TileType.Lava — walkable but deals (4 + floor*2) damage per step
     - Animated red/orange/yellow glow visual (LavaVisual in TileDefinitions)
     - Spawns as small pools (radius 1-2) away from spawn, more on higher floors
     - Dimmed to Red when explored but not visible
     - Death by lava tracked: "lava" as LastKillerName
     - Tile info: "Molten lava — standing here deals damage!"
     - To add hazard tiles: add TileType + visual + generator + TurnManager effect

  4. KILL STREAK COL MULTIPLIER (Systems/TurnManager.cs)
     - Difficulty-scaled bonus at streak milestones (5/10/15 kills, capped at 3 tiers)
     - ColStreakBonus array: Story=8%, Easy=6%, Normal=5%, Hard=4%, VeryHard=3%...
     - Formula: tier (0-3) * ColStreakBonus[difficulty] percent of base Col
     - Displayed in log: "STREAK +X Col (Y%)"
     - Streak resets when player takes damage (existing behavior)
     - To tune: adjust ColStreakBonus array or tier thresholds (_killStreak / 5)

  5. WEAPON REPAIR AT VENDOR (UI/Dialogs/ShopDialog.cs)
     - New "Repair" button in shop dialog alongside Buy/Sell/Leave
     - Repairs all equipped items (Weapon, Chest, Head, Feet) in one click
     - Cost: 2 Col per missing durability point
     - Max durability estimate: 50 + item.RequiredLevel * 10
     - Logs repair results per item or "Nothing to repair" if all gear is full
     - To tune: adjust cost multiplier or max durability formula

================================================================================
  BATCH 20 — SHIELDS, LOOT, COMBAT & QOL
================================================================================

  1. SHIELD / OFF-HAND SYSTEM (Inventory/Core/EquipmentSlot.cs, Items/Equipment/Armor.cs,
     Inventory/Equipment/EquipmentSlotResolver.cs, Systems/TurnManager.cs)
     - New EquipmentSlot.OffHand for shields
     - Armor.BlockChance property (percent, 0-100): chance to fully negate an attack
     - Shield types mapped: "shield", "buckler", "kite shield", "tower shield"
     - Block check runs before dodge in ProcessEntityTurns
     - Blocked attacks degrade the shield (DegradeEquipment on OffHand)
     - Log: "Your {shield} blocks {monster}'s attack!"
     - Equipment pool: roll 5 in CreateRandomEquipment generates shields
       - BlockChance = 10 + floor + Random(0,6), ArmorSlot = "Shield"
     - ShopDialog repairs OffHand slot alongside other equipment
     - To add shield types: add names to ShieldNouns, register in EquipmentSlotResolver

  2. MONSTER LOOT TABLES (Systems/TurnManager.cs, Entities/Mob.cs, Map/MobFactory.cs)
     - Mob.LootTag property links each mob to a loot category
     - MobLootTable dictionary: 10 tags (beast, kobold, insect, plant, humanoid,
       reptile, undead, construct, dragon, elemental) with 3 themed drops each
     - 40% drop chance on kill, creates MobDrop item on tile
     - MobFactory sets LootTag from template for all 25 mob types
     - To add loot: add entries to MobLootTable, add new LootTags to MobFactory

  3. MAP REVEAL ON BOSS KILL (Systems/TurnManager.cs)
     - When a Boss is defeated, all tiles on the current map are set to explored
     - Log: "The floor trembles... the fog lifts!"
     - Minimap shows full layout after boss defeat
     - Implemented via GameMap.SetExplored() loop on boss kill

  4. LEVEL-UP SURGE BUFF (Systems/TurnManager.cs, UI/GameScreen.cs)
     - On level up, player gains +3 ATK/DEF buff for 10 turns
     - _levelUpBuff = 3, _levelUpBuffTurns = 10
     - Buff applied to damage dealt and damage reduction
     - Ticks down in AdvanceTurn alongside other status effects
     - HUD shows [SURGE:N] in the action bar status effects
     - To tune: adjust buff magnitude and duration in ProcessLevelUp

  5. TREASURE ROOM TRAPS (Map/MapGenerator.cs)
     - 35% chance to place TrapSpike tiles adjacent to treasure room loot
     - Traps placed on grass/path tiles within 1 tile of loot position
     - Creates risk-reward dynamic: loot is guarded by spike traps
     - Uses existing TrapSpike tile type and damage system

  6. AGGRO LEASHING (Entities/Mob.cs, Map/GameMap.cs, Systems/SimpleAI.cs)
     - Mob.SpawnX/SpawnY track original spawn position (set in GameMap.PlaceEntity)
     - Mob.LeashRange = 12 tiles (Chebyshev distance from spawn)
     - When mob exceeds LeashRange while chasing, it returns to spawn instead
     - SimpleAI.MoveTowardPoint helper navigates mob back to spawn
     - Prevents kiting mobs across entire map
     - To tune: adjust Mob.LeashRange default or per-mob in MobFactory

  7. AMBIENT SOUND CUES (Systems/TurnManager.cs)
     - SoundCues dictionary maps LootTag to flavor text (e.g., "beast" -> growling)
     - CheckSoundCues() fires once per 5 turns max (SoundCueCooldown)
     - Reports only the closest unseen monster within aggro+2 range
     - Log: "You hear {cue} nearby..." (only if monster not visible)
     - 10 LootTag entries: beast, kobold, insect, plant, humanoid, reptile,
       undead, construct, dragon, elemental
     - To add cues: add entries to SoundCues dictionary

  8. FLOOR RECAP ON ASCEND (Systems/TurnManager.cs)
     - Tracks _floorDamageTaken, _floorItemsFound, _floorKillsStart per floor
     - On AscendFloor, logs a recap: kills, items found, damage taken, turns, explored%
     - Counters reset on each new floor
     - _floorItemsFound incremented on pickup, _floorDamageTaken on player damage

  9. SELL PRICE FLOOR SCALING (UI/Dialogs/ShopDialog.cs)
     - CalcSellPrice now accepts floor parameter
     - Formula: basePrice + (basePrice * max(0, floor-1) * 5%)
     - Higher floors = better sell prices at vendors
     - currentFloor passed from GameScreen via TurnManager.CurrentFloor
     - To tune: adjust the 5% per-floor multiplier

  10. SHIELD DISPLAY IN SHOP (UI/Dialogs/ShopDialog.cs)
      - Shop info panel shows BlockChance for shield items
      - Shield cloning preserves BlockChance property
      - Repair includes OffHand slot alongside Weapon/Chest/Head/Feet

──────────────────────────────────────────────────────────────────────────────
  BATCH 21 — TurnManager Refactor: Extract FlavorText & LootGenerator
──────────────────────────────────────────────────────────────────────────────

  Goal: Reduce TurnManager.cs from 1886 lines to ~1369 by extracting pure-data
  and self-contained logic into dedicated static classes.

  1. FLAVORTEXT EXTRACTION (Systems/FlavorText.cs)
     - New static class holding ALL flavor text arrays, constants, and dictionaries
     - Sections: Combat, Items & Interaction, Movement & Environment,
       Exploration & Auto-Explore, Progression, Atmosphere, Kill Titles
     - Includes constants: FootstepChance, AmbientChance, MilestoneInterval,
       IdleThreshold, IdleRepeatInterval
     - Includes dictionaries: TerrainFlavors (TileType→string[]),
       SoundCues (LootTag→string[])
     - Includes TitleThresholds kill-based title progression array
     - TurnManager references via FlavorText.XxxFlavors pattern
     - Zero coupling — pure data, no dependencies on game state

  2. LOOTGENERATOR EXTRACTION (Systems/LootGenerator.cs)
     - New static class holding loot tables, rarity scaling, and equipment creation
     - MobLootTable: Dictionary<string, (Name,Value)[]> — themed drops per LootTag
     - RarityScaling: stat/durability/value multipliers per rarity tier
     - Equipment name tables: WeaponPrefixes, WeaponNouns, DaggerNouns, etc.
     - PickRarity(): weighted random rarity roll (Common/Uncommon/Rare/Epic)
     - CreateRandomEquipment(int currentFloor): generates floor-scaled gear
     - TurnManager's DropLoot/DropItem stay in TurnManager (need map/log access)

  Files changed:
    - Systems/FlavorText.cs  — NEW (399 lines)
    - Systems/LootGenerator.cs — NEW (140 lines)
    - Systems/TurnManager.cs — Removed ~517 lines of static data, updated all
      references to use FlavorText.* and LootGenerator.* prefixes

──────────────────────────────────────────────────────────────────────────────
  BATCH 22 — Floor-Based Titles & Victory Screen
──────────────────────────────────────────────────────────────────────────────

  1. FLOOR-BASED TITLE PROGRESSION (Systems/FlavorText.cs, Systems/TurnManager.cs)
     - TitleThresholds changed from kill-based to floor-based
     - Thresholds: Floor 1 Adventurer, 2 Blooded, 5 Survivor, 10 Seasoned,
       15 Proven, 25 Dungeon Scourge, 35 Nightmare Walker, 50 Floor Conqueror,
       75 Clearance Hero, 100 Liberator of Aincrad
     - UpdatePlayerTitle() now checks CurrentFloor instead of KillCount
     - Called on AscendFloor() instead of on each kill
     - Titles are distinct from weapon proficiency ranks (which track per-weapon mastery)

  2. VICTORY SCREEN (UI/VictoryScreen.cs)
     - New screen displayed when the player clears all 100 floors
     - "AINCRAD CLEARED" banner, player identity with final title,
       run summary (floors/kills/turns/col/items/level), S+ rating
     - Return to title button (same pattern as DeathScreen)
     - TurnManager.GameWon event fires when AscendFloor() pushes past floor 100
     - AscendFloor() short-circuits before generating floor 101
     - Wired in GameScreen.cs alongside PlayerDied handler

  Files changed:
    - Systems/FlavorText.cs — TitleThresholds now floor-based, added floor 100 title
    - Systems/TurnManager.cs — UpdatePlayerTitle uses CurrentFloor, GameWon event,
      victory check in AscendFloor, title update moved to floor change
    - UI/VictoryScreen.cs — NEW
    - UI/GameScreen.cs — Wired GameWon event to VictoryScreen

──────────────────────────────────────────────────────────────────────────────
  BATCH 23 — UI Improvements (Inventory Comparison & Sell Value)
──────────────────────────────────────────────────────────────────────────────

  1. EQUIPMENT COMPARISON (UI/Helpers/EquipmentComparer.cs)
     - New static helper that builds side-by-side stat diff strings
     - Shows when selecting equipment in inventory: "ATK: 5→12 (+7)  DEF: 3→2 (-1)"
     - Compares against currently equipped item in the matching slot
     - Resolves slots from Weapon/Armor type properties
     - Dedicated comparison line in InventoryDialog below detail line

  2. TOTAL SELL VALUE (UI/Dialogs/InventoryDialog.cs)
     - Footer line showing sum of all inventory item values
     - Updates on every item list change (drop, use, equip)
     - Helps player decide when to visit a vendor

  3. SATIETY BAR IN HUD (UI/GameScreen.cs)
     - Persistent SAT [██░░░░] bar shown next to HP and XP in the action bar
     - Uses block fill characters for visual clarity
     - Always visible — replaces the old [HUNGRY:xx] tag as primary hunger indicator
     - [HUNGRY] status tag still shows when satiety drops below 30

  4. HELP DIALOG — MISSING KEYBINDS (UI/Dialogs/HelpDialog.cs)
     - Added Sprint (Shift+dir), Stealth (Ctrl+dir), Rest (R) to controls list
     - Added Item (!), Fountain (F), Shrine (S), Pillar (P) to map legend
     - Increased dialog height to 34 to fit new entries

  5. MINIMAP LANDMARK MARKERS (UI/MinimapView.cs)
     - Campfire '&' (yellow), Fountain 'F' (cyan), Shrine 'S' (magenta),
       Pillar 'P' (white), Lava '~' (red) now render on the minimap
     - Traps render as '.' (floor) — no spoilers on the minimap
     - Makes landmarks discoverable at a glance on explored terrain

  Files changed:
    - UI/Helpers/EquipmentComparer.cs — NEW (static comparison builder)
    - UI/Dialogs/InventoryDialog.cs — Added comparison line, sell value footer,
      reorganized layout (detail + compare + buttons + sell value)
    - UI/GameScreen.cs — Added SAT bar to action bar HUD
    - UI/Dialogs/HelpDialog.cs — Added missing keybinds + legend entries
    - UI/MinimapView.cs — Added landmark glyphs + colors to terrain tables

────────────────────────────────────────────────────────────────────────────────
  BATCH 24 — UI POLISH & MAP ENHANCEMENTS (10 Features)
────────────────────────────────────────────────────────────────────────────────

  1. Boss HP Bar Overlay
     - Centered HP bar at top of map viewport when a living boss is visible
     - Format: " BOSS_NAME [########----] HP/MaxHP " in BrightRed on DarkGray
     - File: UI/MapView.cs — RenderBossBar() method

  2. Equipment Durability Warnings
     - HUD status line shows [!Wpn:5] style warnings when any equipped item < 10 durability
     - File: UI/GameScreen.cs — RefreshHud() durability scan loop

  3. Floor Theme Color
     - Map frame border color changes by floor tier:
       Green (1-10), Yellow (11-25), BrightRed (26-50), Red (51-75), BrightMagenta (76+)
     - File: UI/GameScreen.cs — FloorThemeColor() helper + FloorChanged handler

  4. Log Scrollback (PageUp/PageDown)
     - Message log can be scrolled with PageUp/PageDown keys
     - Files: UI/MapView.cs (events), UI/ColoredLogView.cs (ScrollPageUp/Down), UI/GameScreen.cs (wiring)

  5. Weapon Proficiency in Stats Dialog
     - Stats dialog (P key) now shows weapon proficiency section below combat stats
     - Per-weapon: type, rank, +damage bonus, progress to next rank
     - File: UI/Dialogs/StatsDialog.cs — accepts optional TurnManager parameter

  6. Run Statistics Dialog (K key)
     - New dialog showing player identity, run summary (floor/kills/turns/col/items/difficulty)
     - Weapon proficiency overview with rank and progress
     - File: UI/Dialogs/KillStatsDialog.cs (NEW)

  7. Danger Compass (Easy difficulties only)
     - Info bar shows nearest monster direction + distance: "Threat: NE(12)"
     - Only on Story/Very Easy/Easy (difficulty 0-2)
     - Files: Systems/TurnManager.cs — GetDangerCompass(), UI/GameScreen.cs — RefreshHud()

  8. Enhanced Look (L key)
     - Monster look info now shows ability tags: [Poison], [Bleed], [Poison,Bleed]
     - Look also shows equipment durability snapshot: "Gear: Wpn:45  Ch:30  Ft:22"
     - Files: Systems/TurnManager.cs — GetTileInfo(), UI/GameScreen.cs — LookRequested handler

  9. Minimap Player Blink
     - Player '@' marker on minimap alternates BrightYellow/White every 3 anim ticks
     - File: UI/MinimapView.cs — player marker color uses TileDefinitions.AnimTick

  10. Map Grid Coordinates
      - Faint DarkGray column/row numbers overlay along map edges every 5th tile
      - Helps orientation on large maps
      - File: UI/MapView.cs — RenderGridCoordinates() method

  Files changed:
    - UI/MapView.cs — Boss bar, grid coordinates, K/PageUp/PageDown events
    - UI/GameScreen.cs — Durability warnings, floor theme, danger compass, log scroll, look durability
    - UI/MinimapView.cs — Player marker blink
    - UI/ColoredLogView.cs — ScrollPageUp/ScrollPageDown methods
    - UI/Dialogs/StatsDialog.cs — Weapon proficiency section, TurnManager param
    - UI/Dialogs/KillStatsDialog.cs — NEW: run statistics dialog
    - Systems/TurnManager.cs — GetDangerCompass(), GetTileInfo() ability tags

────────────────────────────────────────────────────────────────────────────────
  BATCH 25 — UI POLISH, MAP EFFECTS & QOL (10 Features)
────────────────────────────────────────────────────────────────────────────────

  1. Status Effect Icons in HUD
     - Compact status tags: [PSN:3|BLD:2|HGR:15] instead of verbose labels
     - Shows RGN indicator when passive regen is active
     - Extracted to UI/Helpers/StatusTagBuilder.cs (NEW) for maintainability
     - File: UI/GameScreen.cs — RefreshHud() uses StatusTagBuilder.Build()

  2. Death Screen Enhancement
     - Death screen now shows difficulty label and weapon proficiency summary
     - DeathScreen.Show() accepts optional TurnManager parameter
     - Per-weapon: type, rank, +damage bonus displayed in the run summary card
     - File: UI/DeathScreen.cs — BuildSummary() enhanced

  3. Monster Kill Feed
     - Consolidated kill reward into single colored line:
       ">> Kobold Grunt slain! +45 XP +67 Col [Sword +1] PERFECT! STREAK+12"
     - Weapon proficiency rank-up tick shown inline
     - File: Systems/TurnManager.cs — kill reward section in ProcessPlayerMove

  4. Inventory Sort Toggle
     - Sort button cycles: Default → Type → Rarity → Name → Value
     - Header shows current sort mode: "Items (5/20) [Sort: Rarity]"
     - Rarity sort orders: Epic > Rare > Uncommon > Common
     - File: UI/Dialogs/InventoryDialog.cs — SortMode enum, RarityOrder helper

  5. Footstep Trail
     - Last 10 player positions rendered as dim '·' dots on visible floor tiles
     - Trail clears on floor change (via SetMap)
     - Only shown on walkable tiles with no occupant or items
     - File: UI/MapView.cs — _footsteps queue, RenderFootstepTrail()

  6. Room Reveal Flash
     - Newly explored tiles flash White-on-DarkGray for one render frame
     - Creates a satisfying "room reveal" effect when entering new areas
     - GameMap.NewlyRevealed set populated during UpdateVisibility
     - Files: Map/GameMap.cs — NewlyRevealed HashSet, UI/MapView.cs — RenderRevealFlash()

  7. Water Reflection Effect
     - Water tiles adjacent (below) to entities show dimmed entity symbol
     - Purely cosmetic shimmer effect — DarkGray on water background
     - File: UI/MapView.cs — GetReflectedEntity(), integrated in ResolveTileVisual

  8. Torch Radius Visualization
     - Light sources now use distance-based gradient instead of flat DarkGray
     - Inner ring (dist 0-1): DarkGray background glow
     - Outer ring (dist 2): subtle foreground brightening
     - File: UI/MapView.cs — GetLightSourceDistance() replaces IsNearLightSource()

  9. XP Progress Bar in Stats Panel
     - Stats panel appends "── Progress ──" section with XP bar:
       "XP [======--------] 340/500"
     - Uses existing BarBuilder.BuildXp() for consistency
     - File: UI/GameScreen.cs — RefreshHud() appends XP bar to stats display

  10. Floor Timer
      - Info bar shows elapsed turns on current floor: "T:47"
      - Resets automatically each floor change (uses _floorStartTurn)
      - TurnManager.FloorTurns computed property added
      - Files: Systems/TurnManager.cs — FloorTurns property, UI/GameScreen.cs — info line

  Files changed:
    - UI/Helpers/StatusTagBuilder.cs — NEW: compact status effect tag builder
    - UI/GameScreen.cs — Status tags, XP bar, floor timer, death screen wiring
    - UI/DeathScreen.cs — Difficulty + weapon proficiency in summary card
    - UI/MapView.cs — Footstep trail, room reveal flash, water reflections, torch gradient
    - UI/Dialogs/InventoryDialog.cs — Sort toggle with 5 modes
    - Map/GameMap.cs — NewlyRevealed HashSet for room reveal flash
    - Systems/TurnManager.cs — Kill feed consolidation, FloorTurns property

================================================================================
  BATCH 26 — UI Polish & Visual Feedback (10 features)
================================================================================

  1. Damage Numbers Flash
     - Combat damage appears as floating numbers on the map for 3 render frames
     - Player damage shown in bright red, monster damage in bright yellow
     - Numbers render one tile above the combat location
     - Events: TurnManager.DamageDealt fires with (x, y, damage, isPlayerDamage)
     - Files: UI/MapView.cs — AddDamageFlash(), RenderDamageFlashes()
             Systems/TurnManager.cs — DamageDealt event

  2. Inventory Weight/Encumbrance Display
     - BaseItem now has a Weight property (default 1)
     - Inventory dialog footer shows total weight alongside sell value
     - Format: "Total sell value: 120 Col  |  Weight: 15"
     - Files: Items/BaseItem.cs — Weight property
             Items/Equipment/Armor.cs — `new` keyword for existing Weight override
             UI/Dialogs/InventoryDialog.cs — weight sum in footer

  3. Combat Log Coloring Enhancement
     - 15+ new keyword color rules added to ColoredLogView:
       Combos/streaks (BrightCyan), kill messages (BrightYellow),
       boss kills (BrightMagenta), status effects (Green/Red/Yellow),
       near-death warnings (BrightRed), proficiency (BrightMagenta),
       floor transitions (BrightCyan), lava damage (BrightRed)
     - File: UI/ColoredLogView.cs — expanded KeywordRules array

  4. Contextual Action Hints
     - Info bar shows context-sensitive hints based on surroundings:
       "[G] Pick up" when items underfoot, "[Bump] Shop" near vendor,
       "[Bump] Attack" near monster, "[Bump] Ascend" on stairs
     - TurnManager.GetContextHint() checks underfoot then adjacent tiles
     - Files: Systems/TurnManager.cs — GetContextHint()
             UI/GameScreen.cs — hint appended to info line

  5. Loot Sparkle
     - Items on the ground shimmer: every 4th animation tick, '!' flashes white
     - Creates a twinkling effect to draw attention to loot drops
     - File: UI/MapView.cs — RenderLootSparkle()

  6. Wall Edge Shading
     - Walls adjacent to open floor render as bright White '#'
     - Interior walls (surrounded by other walls) stay dimmer Gray/DarkGray
     - Creates a 3D depth effect — room edges pop out
     - File: UI/MapView.cs — IsWallEdge() helper, post-effect in ResolveTileVisual

  7. Explored Room Tint
     - Explored-but-not-visible floor tiles render in Gray instead of DarkGray
     - Rooms stand out more clearly in map memory vs corridors
     - File: UI/MapView.cs — Layer 3 (explored not visible) special case for Floor

  8. Corpse Markers
     - Dead monsters leave a dim '%' marker at their death location
     - Fades after ~40 render cycles (~30 seconds)
     - Hidden if another entity occupies the tile or items are present
     - Events: TurnManager.MonsterKilled fires with (x, y)
     - Files: UI/MapView.cs — AddCorpseMarker(), RenderCorpseMarkers()
             Systems/TurnManager.cs — MonsterKilled event

  9. Auto-Save Indicator
     - "[Saved]" tag appears in the action bar for 5 turns after each floor change
     - Provides visual feedback that progress was preserved on descent
     - File: UI/GameScreen.cs — saveFlashTurns counter, appended to HP label

  10. Minimap Threat Dots
      - Minimap now differentiates monster threat levels by color:
        Higher-level monsters pulse BrightRed/BrightYellow (dangerous warning)
        Even-level monsters show solid Red
        Lower-level monsters show dim DarkGray
      - Bosses display as pulsing 'B' instead of generic '*'
      - File: UI/MinimapView.cs — enhanced SampleRegion entity rendering

  Files changed:
    - Items/BaseItem.cs — Weight property added
    - Items/Equipment/Armor.cs — `new` keyword for Weight override
    - UI/MapView.cs — Damage flash, corpse markers, loot sparkle, wall edge shading,
                       explored room tint (6 new overlay/effect systems)
    - UI/MinimapView.cs — Threat-colored monster dots, boss pulse
    - UI/ColoredLogView.cs — 15+ new keyword color rules
    - UI/GameScreen.cs — Context hints, auto-save indicator, damage/corpse event wiring
    - UI/Dialogs/InventoryDialog.cs — Weight display in footer
    - Systems/TurnManager.cs — DamageDealt/MonsterKilled events, GetContextHint()

================================================================================
  BATCH 27 — Map & Graphics Enhancement (10 features)
================================================================================

  1. Fog of War Edge Gradient
     - Explored tiles bordering unexplored areas get a dim treatment
     - 3+ unexplored neighbors = DarkGray (outer edge), 1-2 = Gray (inner edge)
     - Creates a soft boundary instead of a hard explored/unexplored cutoff
     - File: UI/Helpers/MapEffects.cs — GetFogEdgeColor()
             UI/MapView.cs — applied in ResolveTileVisual Layer 2

  2. Ambient Particle Effects
     - Sparse animated particles on certain terrain types (~5% of tiles per frame)
     - Grass: drifting '·' dots (DarkGray) — wind effect
     - Water: rising apostrophe (Blue) — mist effect
     - Campfire: alternating '*'/'·' (BrightYellow) — ember sparks
     - Position hash + AnimTick for deterministic, slowly shifting particles
     - Files: UI/Helpers/MapEffects.cs — GetAmbientParticle()
              UI/MapView.cs — RenderAmbientParticles() overlay pass

  3. Door Open/Closed State
     - Doors start closed ('+' Yellow) and open when walked through ('/' Brown)
     - Closed doors block line-of-sight; opened doors are transparent
     - State tracked in both MapView (visual) and GameMap (LOS calculation)
     - Files: UI/MapView.cs — _openedDoors HashSet, MarkDoorOpened()
              Map/GameMap.cs — OpenedDoors HashSet, HasLineOfSight() check
              UI/GameScreen.cs — syncs door state on TurnCompleted

  4. Terrain Transition Borders
     - Ground tiles adjacent to water show ',' in Blue (shoreline mud)
     - Ground tiles adjacent to lava show ',' in Red (scorched edge)
     - Only affects Floor, Path, Grass, GrassSparse, GrassTall tiles
     - File: UI/Helpers/MapEffects.cs — GetTransitionBorder()

  5. Shadow Casting
     - Non-wall tiles directly below a wall tile render with DarkGray background
     - Creates a 1-tile drop shadow beneath walls for depth perception
     - Walls and mountains themselves are excluded from shadow treatment
     - File: UI/Helpers/MapEffects.cs — IsInWallShadow()

  6. Shrine Aura Glow
     - Shrine tiles emit a mystical purple glow within 2-tile Manhattan distance
     - Distance 1: Magenta foreground tint + DarkGray background
     - Distance 2: detected but no visual change (expansion-ready)
     - File: UI/Helpers/MapEffects.cs — GetShrineDistance()

  7. Health-Based Player Color
     - Player '@' symbol changes color based on current HP percentage:
       Above 75%: BrightYellow (healthy — default)
       50-75%: Yellow (caution)
       25-50%: BrightRed (danger)
       Below 25%: Pulsing Red/DarkGray (critical — flashing warning)
     - File: UI/MapView.cs — ResolveTileVisual entity rendering

  8. Water Edge Foam
     - Water tiles adjacent to land render in BrightCyan instead of standard Blue
     - Creates a visible shoreline/foam effect at water boundaries
     - Checks all 8 neighbors; any non-water/non-wall neighbor triggers foam
     - File: UI/Helpers/MapEffects.cs — IsWaterEdge()

  9. Room Lighting Variation
     - Floor tiles receive a subtle color tint based on their 8x8 region
     - Regions hash to one of: neutral Gray, warm Gold (BrightYellow),
       cool Blue (BrightCyan), overgrown Green (BrightGreen), or no tint
     - Only applies to tiles with neutral Gray foreground (avoids overriding
       entity colors or other effects)
     - File: UI/Helpers/MapEffects.cs — GetRoomTint()

  10. Depth-Based Map Tinting
      - Tiles at medium distance from player (falloff 0.45-0.75) get dimmer colors
      - Bright colors map to their dim equivalents:
        White→Gray, BrightGreen→Green, BrightBlue→Blue, BrightCyan→Cyan,
        BrightYellow→Yellow, Green→DarkGray, Gray→DarkGray
      - Creates a gradual distance fade before hard visibility cutoff
      - File: UI/Helpers/MapEffects.cs — GetDepthTintedColor()

  New file created:
    - UI/Helpers/MapEffects.cs — 8 static methods for map visual post-effects.
      Extracted from MapView to keep the main renderer focused on core tile logic.
      All methods are pure functions taking map state and returning visual overrides.

  Files changed:
    - UI/Helpers/MapEffects.cs — NEW: 8 static visual effect helpers
    - UI/MapView.cs — Door state tracking, ambient particle overlay,
                       ResolveTileVisual rewritten with all 10 new effects integrated
    - Map/GameMap.cs — OpenedDoors HashSet, closed doors block LOS in HasLineOfSight()
    - UI/GameScreen.cs — Door state sync on TurnCompleted event

================================================================================
  BATCH 28 — Weather, Visual Effects & UI Enhancements (10 features)
================================================================================

  1. Minimap Fog Reveal Animation
     - Newly explored minimap cells pulse white/cyan for 6 animation frames
     - RevealedCells HashSet tracks which minimap cells were just discovered
     - Triggers on GameMap.NewlyRevealed, clears after flash duration
     - File: UI/MinimapView.cs — _revealedCells, _revealFlashCounter

  2. Entity Footprint Trails (Mob Patrol Paths)
     - Monsters leave dim '·' trail markers showing their last 4 positions
     - Per-entity trail queues stored in MapView._mobTrails dictionary
     - Trails are tracked on each TurnCompleted, cleared on monster death
     - Only visible on currently-visible, unoccupied, walkable tiles
     - File: UI/MapView.cs — TrackMobPosition(), ClearMobTrail(), RenderMobTrails()

  3. Weather System (Clear/Rain/Fog/Wind)
     - WeatherType enum: Clear (40%), Rain (25%), Fog (15%), Wind (20%)
     - Rolled via weighted random on each floor change (AscendFloor + Floor 1)
     - Rain: blue particle overlay ('|' and '/'), desaturates bright colors
     - Fog: reduces effective visibility radius by 3 tiles
     - Weather label shown in HUD when not Clear (e.g. "[Rainy]")
     - Announced in combat log on floor entry
     - File: Systems/WeatherSystem.cs — NEW static class
     - File: UI/Helpers/MapEffects.cs — GetRainParticle(), GetWeatherDimmedColor()
     - File: UI/MapView.cs — RenderRainParticles(), fog visibility in ResolveTileVisual
     - File: Systems/TurnManager.cs — WeatherSystem.RollWeather() in AscendFloor()
     - File: UI/GameScreen.cs — Weather label in RefreshHud(), initial roll for Floor 1

  4. Tile Damage Scorch Marks
     - '░' scorch marks appear where trap or lava damage occurred
     - Fade over 60 render frames: first half Red, second half DarkGray
     - Placed via DamageDealt event when isPlayerDamage is true (traps)
     - Also placed when player stands on Lava tiles
     - Spike trap now fires DamageDealt event for visual feedback
     - File: UI/MapView.cs — _scorchMarks, AddScorchMark(), RenderScorchMarks()
     - File: Systems/TurnManager.cs — DamageDealt invoke on spike trap
     - File: UI/GameScreen.cs — Scorch mark on player damage + lava tiles

  5. Animated Water Flow (Wave Propagation)
     - Water tiles cycle between '~' and '≈' glyphs based on position+time hash
     - Creates a flowing ripple pattern across water bodies
     - Only applies when no entity reflection is being shown
     - File: UI/Helpers/MapEffects.cs — GetWaterFlowGlyph()
     - File: UI/MapView.cs — ResolveTileVisual water rendering

  6. Death Recap Log
     - Death screen shows "Last Moments" box with the last 5 combat log entries
     - Boxed card displayed above the return button
     - ColoredLogView exposes GetRecentEntries(count) for external access
     - DeathScreen.Show() accepts optional ColoredLogView parameter
     - File: UI/ColoredLogView.cs — GetRecentEntries()
     - File: UI/DeathScreen.cs — Death recap section, new logView parameter
     - File: UI/GameScreen.cs — Passes coloredLog to DeathScreen.Show()

  7. Monster Awareness Indicators
     - Pulsing '!' rendered above monsters within 6-tile chase range
     - Alternates BrightRed/BrightYellow on AnimTick for attention
     - Only shows for visible monsters (fog of war respected)
     - File: UI/Helpers/MapEffects.cs — ShouldShowAggroIndicator()
     - File: UI/MapView.cs — RenderAggroIndicators()

  8. Explored Area Heatmap
     - Per-tile visit counter tracks how many times player walked on each tile
     - MapEffects.GetHeatmapColor() maps visit counts to colors:
       1+: DarkGray, 3+: Yellow, 5+: Red, 10+: BrightRed
     - Only affects floor/path/grass tiles
     - Visit count stored in GameMap._visitCounts int[,] array
     - File: Map/GameMap.cs — _visitCounts, IncrementVisit(), GetVisitCount()
     - File: UI/Helpers/MapEffects.cs — GetHeatmapColor()
     - File: Systems/TurnManager.cs — IncrementVisit() on player move

  9. Campfire Light Flicker
     - Campfire light radius pulses between 2 and 3 tiles
     - Per-campfire phase offset via position hash (not all campfires sync)
     - Tiles within flicker radius get DarkGray background (close) or
       Yellow foreground boost (at distance)
     - File: UI/Helpers/MapEffects.cs — GetCampfireFlickerRadius(),
             GetCampfireGlowDistance()
     - File: UI/MapView.cs — ResolveTileVisual campfire glow post-effect

  10. Status Effect Screen Tint
      - Active poison tints every 3rd tile green
      - Active bleed tints every 3rd tile red
      - Both active: magenta tint
      - Subtle scanline pattern (only (mx+my)%3==0 tiles affected)
      - Cleared automatically when status effects expire
      - File: UI/MapView.cs — _statusTintColor, SetStatusTint(), ResolveTileVisual
      - File: UI/GameScreen.cs — Status tint update in TurnCompleted handler

  New file created:
    - Systems/WeatherSystem.cs — Weather enum, weighted random roll, fog reduction

  Files changed:
    - UI/Helpers/MapEffects.cs — 7 new static methods: GetRainParticle,
      GetWeatherDimmedColor, GetWaterFlowGlyph, GetCampfireFlickerRadius,
      GetCampfireGlowDistance, ShouldShowAggroIndicator, GetHeatmapColor
    - UI/MapView.cs — 6 new overlay methods, scorch/mob trail/status tint state,
      ResolveTileVisual updated with campfire flicker, weather fog, status tint
    - UI/MinimapView.cs — Fog reveal animation with pulsing white/cyan cells
    - UI/ColoredLogView.cs — GetRecentEntries() public method
    - UI/DeathScreen.cs — Death recap "Last Moments" card, logView parameter
    - UI/GameScreen.cs — Weather HUD label, status effect tint wiring,
      scorch mark + mob trail event handlers, death screen log passthrough
    - Map/GameMap.cs — _visitCounts array, IncrementVisit(), GetVisitCount()
    - Systems/TurnManager.cs — Weather roll on floor change, visit tracking,
      trap DamageDealt event

================================================================================
  BATCH 29 — Unicode Visual Upgrades (8 features)
================================================================================

  1. Mountain Unicode Glyphs
     - Mountains now render as ▲ (large, 67%) and △ (small, 33%) instead of '^'
     - Color variation: Gray, White, DarkGray across the range
     - File: Map/TileType.cs — MountainVisual() replaces MountainColor()

  2. Wall Shading Blocks
     - Inner walls render as ▓ (medium shade block) — base TileDefinitions
     - Edge walls (adjacent to open floor) render as █ (full block) — MapView override
     - Creates a 3D depth effect: solid borders with textured interiors
     - File: Map/TileType.cs — WallVisual() replaces flat '#'
     - File: UI/MapView.cs — Edge wall override changed from color-only to glyph+color

  3. Door Unicode Glyphs
     - Closed doors: ▌ (left half block) — suggests a solid barrier
     - Opened doors: ╌ (broken horizontal line) — suggests a passage
     - Replaces '+' (closed) and '/' (opened)
     - File: Map/TileType.cs — Door glyph changed to '▌'
     - File: UI/MapView.cs — Opened door glyph changed to '╌'

  4. Item Rarity Markers
     - Ground items now show rarity-specific Unicode glyphs instead of flat '!':
       Common: • (BrightYellow), Uncommon: ◇ (BrightGreen),
       Rare: ◆ (BrightCyan), Epic: ★ (BrightYellow), Legendary: ★ (BrightMagenta)
     - Highest-rarity item on tile determines the marker shown
     - Loot sparkle overlay also uses rarity-appropriate glyphs
     - File: UI/MapView.cs — GetItemRarityVisual(), updated ResolveTileVisual +
             RenderLootSparkle

  5. Fog Density Gradient
     - Fog edge tiles now render with ░▒▓ block shading based on density:
       1-2 unexplored neighbors: ░ (light), 3-4: ▒ (medium), 5+: ▓ (thick)
     - Overlays the fog edge color dimming for a smooth visual transition
     - File: UI/Helpers/MapEffects.cs — GetFogDensityGlyph()
     - File: UI/MapView.cs — Fog edge section applies density glyph

  6. Lava Bubble Animation
     - Lava tiles cycle between ▓ ▒ ░ block elements instead of flat '~'
     - Combined with Red/BrightRed/BrightYellow color cycling for roiling magma effect
     - 6-phase animation loop: dense→medium→light blocks
     - File: Map/TileType.cs — LavaVisual() updated with block glyphs

  7. Campfire Ember Symbols
     - Campfire ambient particles upgraded from '*'/'·' to Unicode: • ∘ ◦
     - Size-cycling animation: large→medium→small, simulating ember lifecycle
     - File: UI/Helpers/MapEffects.cs — GetAmbientParticle() campfire case

  8. Water Depth Shading
     - Shallow water alternates between ░ (depth shade) and ~ (flow)
     - Deep water alternates between ▒ (denser shade) and ~ (flow)
     - Combined with existing color cycling for layered depth+motion effect
     - File: Map/TileType.cs — WaterVisual() and WaterDeepVisual() updated

  Files changed:
    - Map/TileType.cs — Mountain, wall, door, lava, water visuals upgraded to Unicode
    - UI/MapView.cs — Wall edge █ override, door ╌ glyph, item rarity markers,
      fog density glyph overlay, loot sparkle rarity glyphs
    - UI/Helpers/MapEffects.cs — GetFogDensityGlyph(), campfire ember Unicode

================================================================================
  BATCH 30 — Visual Polish & UI Glyph Upgrades (9 features)
================================================================================

  1. Tree Species Glyphs
     - Deciduous trees: ♣ (club suit) replaces 'T', color variation preserved
     - Pine trees: ▲ (filled triangle) replaces 'T', green/dark variation
     - Bushes: ♠ (spade suit) replaces '*', green color variation preserved
     - File: Map/TileType.cs — Tree/TreePine/Bush glyph updates + TreePineVisual()

  2. Flower Variety Glyphs
     - Flowers now use 3 Unicode glyphs: ✿ ❀ ✻ (deterministic per-tile via hash)
     - Animated color cycling preserved (BrightYellow, BrightRed, BrightMagenta, BrightCyan)
     - File: Map/TileType.cs — FlowerGlyphs array + FlowerVisual() updated

  3. Shrine/Fountain/Pillar Unicode Upgrades
     - Shrine: animated ☥/✦ (ankh/star) with magenta/cyan color cycling
     - Fountain: animated ⊙/◉ (ring/dot) with blue/cyan color cycling
     - Pillar: ║ (double vertical line) replaces '|', BrightCyan
     - File: Map/TileType.cs — ShrineVisual(), FountainVisual() updated, Pillar glyph

  4. Trap Glyph Upgrades
     - Spike trap: ⚠ (warning sign) replaces '^', DarkGray to stay subtle
     - Teleport trap: ◎ (bullseye) replaces '*', DarkGray to stay subtle
     - File: Map/TileType.cs — TrapSpike/TrapTeleport glyph updates

  5. Stairs Glyph Upgrade
     - Stairs up: ≫ (double greater-than), BrightCyan on DarkGray
     - StairsDown removed — players ascend Aincrad (Floor 1 → 100)
     - Bolder glyphs make stairs harder to miss during exploration
     - File: Map/TileType.cs — StairsUp glyph update

  6. HUD Bar Gradient Fill
     - HP, XP, and all stat bars now use █▓▒░ block element gradient
     - Full cells = █, trailing edge = ▓ (75%+), ▒ (50%+), ░ (25%+)
     - Empty cells use · (middle dot) for cleaner look than '.' or '-'
     - Boss HP bar also uses gradient fill
     - File: UI/Helpers/BarBuilder.cs — BuildGradient(), BuildHp(), BuildXp() rewritten
     - File: UI/MapView.cs — Boss bar uses BarBuilder.BuildGradient()

  7. Box-Drawing Helper Library
     - New BoxDrawing.cs utility with Single (┌─┐│└─┘) and Double (╔═╗║╚═╝) char sets
     - Helper methods: HRule, TitleRow, ContentRow, BottomRow, DoubleTitleRow
     - Standardizes box-drawing across all UI panels and cards
     - File: UI/Helpers/BoxDrawing.cs (NEW)

  8. Status Effect Icons in HUD
     - Each status tag now prefixed with a Unicode icon:
       ☠PSN (poison), ♦BLD (bleed), ✧HGR (hungry)
       ◈BLS (shrine buff), ◌STL (stealth), ✦SRG (surge), ♥RGN (regen)
     - Makes status effects immediately visually scannable
     - File: UI/Helpers/StatusTagBuilder.cs — Tags updated with Unicode prefixes

  9. Animated Sparkle for Interactables
     - Doors, stairs, shrines, fountains, pillars, campfires pulse ✦/✧ sparkle
     - ~25% visibility per frame, offset by position hash (non-synchronized)
     - Color matches tile type: magenta (shrine), cyan (fountain), yellow (campfire/door)
     - File: UI/Helpers/MapEffects.cs — GetInteractableSparkle()
     - File: UI/MapView.cs — RenderInteractableSparkle() post-render overlay

  Files changed:
    - Map/TileType.cs — Trees, flowers, shrines, fountains, pillars, traps, stairs
    - UI/Helpers/BarBuilder.cs — Gradient bar system (█▓▒░· fill)
    - UI/Helpers/BoxDrawing.cs (NEW) — Unicode box-drawing character library
    - UI/Helpers/StatusTagBuilder.cs — Unicode icon prefixes for status effects
    - UI/Helpers/MapEffects.cs — GetInteractableSparkle() method
    - UI/MapView.cs — Boss bar gradient + RenderInteractableSparkle overlay

  New file:
    - UI/Helpers/BoxDrawing.cs

================================================================================
  BATCH 31 — Visual Depth & Atmosphere (10 features)
================================================================================

  1. Corpse Marker Upgrade
     - Dead monsters now show '†' (cross) instead of '%' at death locations
     - Color fades over lifetime: Red (fresh) → Gray (mid) → DarkGray (old)
     - Lifespan still 40 render frames (~30 seconds)
     - SAO-thematic: no blood/gore, just a grave marker
     - File: UI/MapView.cs — RenderCorpseMarkers() updated glyph + color fade

  2. Water Lily Pads
     - Static green '●' decorations appear on ~10% of shallow water tiles
     - Position-hash-based, deterministic per tile (no animation)
     - Only on TileType.Water (not WaterDeep) — shallow ponds only
     - Does not render when a water reflection is active
     - File: UI/Helpers/MapEffects.cs — GetWaterLilyPad()
     - File: UI/MapView.cs — Integrated in ResolveTileVisual water section

  3. Ambient Sound Text
     - ~1% chance per turn to log soft flavor text near terrain features
     - Water: "...water laps gently against the shore." (3 variants)
     - Campfire: "...the campfire crackles softly." (3 variants)
     - Wind: "...a cool breeze rustles through the area." (3 variants)
     - Scales down near large water bodies (50% skip if 10+ water tiles nearby)
     - File: UI/Helpers/MapEffects.cs — GetAmbientSoundText()
     - File: UI/GameScreen.cs — Called in TurnCompleted handler

  4. Minimap Unicode Upgrade
     - Walls: '▪' (small filled square) replaces '#'
     - Water/Lava: '≈' (approximate equal) replaces '~'
     - Stairs: '◊' (diamond) replaces '>' and '<'
     - Boss: '◆' (filled diamond) replaces 'B'
     - All other terrain glyphs unchanged
     - File: UI/MinimapView.cs — GetTerrainGlyph() + boss marker updated

  5. Kill Streak Visual Flash
     - On Double Kill (2+ streak): player '@' flashes BrightMagenta for 3 frames
     - On Triple Kill (3+ streak): additional '★' burst in 8 directions (BrightYellow)
     - Triggered via TurnManager.KillStreak public property
     - File: UI/MapView.cs — TriggerKillStreakFlash(), RenderKillStreakFlash()
     - File: Systems/TurnManager.cs — KillStreak public property exposed
     - File: UI/GameScreen.cs — Wired in MonsterKilled handler

  6. Lava Danger Zone Tint
     - Floor tiles within 2 Manhattan distance of lava get DarkGray background
     - Creates a visual heat warning zone around lava pools
     - Walls and lava tiles themselves are excluded
     - No trap warnings — danger zones are lava-only (traps stay unexpected)
     - File: UI/Helpers/MapEffects.cs — IsInLavaDangerZone()
     - File: UI/MapView.cs — Post-effect in ResolveTileVisual

  7. Explored Room Glow
     - Floor tiles with all 4 cardinal neighbors explored get a warm Yellow tint
     - Only applies to tiles with neutral Gray foreground (avoids overriding effects)
     - Gives well-explored rooms a "safe/familiar" visual feeling
     - File: UI/Helpers/MapEffects.cs — GetExploredRoomGlow()
     - File: UI/MapView.cs — Post-effect in ResolveTileVisual

  8. [SIDELINED] Entity Shadow — removed after playtesting

  9. Shatter Particles (SAO Polygon Burst)
     - On monster death, '◇' diamond fragments radiate outward from death position
     - 4-frame animation: expanding ring, color fades BrightCyan → Cyan → DarkGray
     - SAO-thematic: monsters shatter into light polygons (no blood/gore)
     - 8-directional burst pattern using ShatterOffsets array
     - File: UI/MapView.cs — AddShatterParticle(), RenderShatterParticles()
     - File: UI/GameScreen.cs — Wired in MonsterKilled handler

  10. Cobweb Decorations
      - Static gray '%' cobwebs on ~15% of wall corner tiles
      - "Corner" = wall adjacent to 2+ non-wall/non-mountain tiles
      - Position-hash-based, deterministic (no animation)
      - Adds dungeon atmosphere without visual noise
      - File: UI/Helpers/MapEffects.cs — GetCobwebDecoration()
      - File: UI/MapView.cs — Post-effect in ResolveTileVisual (before wall edge shading)

  Files changed:
    - UI/MapView.cs — 4 new overlay methods (shatter particles,
      kill streak flash, cobweb post-effect, lily pad integration),
      corpse marker upgraded to '†' with color fade, lava/room glow post-effects
    - UI/Helpers/MapEffects.cs — 5 new static methods (GetWaterLilyPad,
      IsInLavaDangerZone, GetCobwebDecoration, GetExploredRoomGlow,
      GetAmbientSoundText)
    - UI/MinimapView.cs — Unicode glyphs for walls/water/stairs/boss
    - UI/GameScreen.cs — Shatter + kill streak wiring in MonsterKilled,
      ambient sound text in TurnCompleted
    - Systems/TurnManager.cs — KillStreak public property

================================================================================
  BATCH 32 — QoL: Equipment Dialog
================================================================================

  1. EQUIPMENT DIALOG (UI/Dialogs/EquipmentDialog.cs)
     - New dedicated equipment overview screen, opened with T key
     - Custom EquipmentSlotView renderer with per-slot colored ASCII icons:
       / Weapon (bright yellow), + Off-Hand (bright cyan),
       ^ Head, # Chest, = Legs, _ Feet (white),
       o Rings (bright magenta), o Bracelet/Necklace (bright green)
     - Rarity-colored item names: Epic=magenta, Rare=cyan, Uncommon=green
     - Selection highlight with DarkGray background
     - Detail line shows level, rarity, damage/defense, stat bonuses, durability
     - Unequip button returns item to inventory (checks if inventory full)
     - Gear stat totals row (ATK, DEF, SPD, STR, VIT, DEX, AGI)
     - Lowest durability warning line
     - Dynamic dialog width: Math.Clamp(maxLen + 10, 48, 80)
     - File: UI/Dialogs/EquipmentDialog.cs (NEW) — includes EquipmentSlotView class

  2. T KEY BINDING (UI/MapView.cs)
     - EquipmentRequested event added to MapView
     - T key fires the event, wired in GameScreen to open EquipmentDialog
     - HUD refreshes after dialog closes (stat changes from unequipping)

  3. BLACK DIALOG BACKGROUNDS (all dialogs)
     - Added ColorSchemes.Dialog (gray-on-black) to ColorSchemes.cs
     - Applied to all 6 dialogs: Equipment, Inventory, Help, Stats,
       KillStats, Shop — consistent black background matching terminal

  4. SPLIT-PANE INVENTORY (UI/Dialogs/InventoryDialog.cs)
     - Redesigned inventory dialog with side-by-side layout (80 wide):
       Left pane (32 cols): EquipmentSlotView showing all 10 equipped slots
       with colored icons, gear stat totals below
       Right pane (remaining): scrollable item list with sort/rarity tags
     - Tab key switches focus between equipment and inventory panes
     - Selecting equipment slot shows equipped item detail in bottom area
     - Selecting inventory item shows item detail + comparison vs equipped
     - Fixed Terminal.Gui ListWrapper.RenderUstr crash (uniform padding)
     - Buttons: Use, Equip, Drop, Sort, Close — all span full width

  5. SHARED EquipmentSlotView (UI/Helpers/EquipmentSlotView.cs)
     - Extracted from EquipmentDialog into standalone public class
     - Reused by both InventoryDialog (left pane) and EquipmentDialog (T key)
     - DefaultSlotLayout: static slot definitions with Unicode icons + colors
     - GetRarityColor(): public static rarity-to-color mapping

  6. ENTITY SHADOWS CLEANUP (UI/MapView.cs)
     - Removed empty "ENTITY SHADOWS" section header left from sidelined feature

  Files changed:
    - UI/Helpers/EquipmentSlotView.cs — NEW: shared equipment slot renderer
    - UI/Dialogs/EquipmentDialog.cs — Uses shared EquipmentSlotView, helpers
      made internal for reuse by InventoryDialog
    - UI/Dialogs/InventoryDialog.cs — Full rewrite: split-pane layout
    - UI/Dialogs/HelpDialog.cs — T keybind entry, black dialog background
    - UI/Dialogs/StatsDialog.cs — Black dialog background
    - UI/Dialogs/KillStatsDialog.cs — Black dialog background
    - UI/Dialogs/ShopDialog.cs — Black dialog background
    - UI/Helpers/ColorSchemes.cs — NEW: ColorSchemes.Dialog palette
    - UI/MapView.cs — EquipmentRequested event, T keybind, shadow header cleanup
    - UI/GameScreen.cs — Wired EquipmentRequested → EquipmentDialog.Show

  7. NO-DEFAULT-HIGHLIGHT FOR EQUIPMENT SLOTS (UI/Helpers/EquipmentSlotView.cs)
     - Equipment slot list no longer highlights first slot on dialog open
     - _selectedIndex starts at -1 (no selection) instead of 0
     - First arrow/WASD press activates highlight at index 0
     - Affects both Inventory (I key) and Equipment (T key) dialogs

────────────────────────────────────────────────────────────────────────────────
  BATCH 33 — Ascend Aincrad (Descend → Ascend Lore Correction)
────────────────────────────────────────────────────────────────────────────────

  Aincrad is climbed from Floor 1 (bottom) to Floor 100 (top). All
  "descend" references corrected to "ascend" throughout the codebase.

  1. TILETYPE CLEANUP (Map/TileType.cs)
     - Removed TileType.StairsDown enum value (unused — progression stairs are StairsUp)
     - StairsUp glyph changed to ≫ (BrightCyan on DarkGray)

  2. METHOD RENAME (Systems/TurnManager.cs)
     - DescendFloor() → AscendFloor()
     - "Descending to Floor X" → "Ascending to Floor X"
     - Boss gate comment: "before descending" → "before ascending"
     - Context hint: "[Bump] Descend" → "[Bump] Ascend"
     - Look description: "Stairs leading down" → "Stairs leading up"

  3. UI TEXT CORRECTIONS
     - GameScreen.cs: "Descend to Floor X?" → "Ascend to Floor X?"
     - ColoredLogView.cs: "Descending to" → "Ascending to" highlight
     - HelpDialog.cs: "Stairs down" → "Stairs up" in map legend
     - TitleScreen.cs: "deeper floors" → "higher floors", "descending" → "ascending"

  4. FLAVOR TEXT CORRECTIONS (Systems/FlavorText.cs)
     - Stairs discovery messages rewritten (upward/ascend instead of deeper/descend)
     - NPC dialogue: "deeper in" → "the floor above"
     - Ambient messages: "somewhere deeper" → "somewhere above"
     - Milestone: "turns deep" → "turns in"
     - Idle flavor: "deeper in the dungeon" → "the floors above"

  5. ALL STAIRSDOWN REFERENCES REMOVED
     - MapGenerator.cs, MapView.cs, MinimapView.cs, MapEffects.cs — all now use StairsUp only

  Files changed:
    - Map/TileType.cs — Removed StairsDown, updated StairsUp glyph
    - Map/MapGenerator.cs — StairsDown → StairsUp
    - Systems/TurnManager.cs — DescendFloor → AscendFloor + all text
    - Systems/FlavorText.cs — All stairs/deeper text corrected
    - UI/GameScreen.cs — Ascend confirmation dialog
    - UI/ColoredLogView.cs — Ascending highlight
    - UI/Dialogs/HelpDialog.cs — Map legend
    - UI/TitleScreen.cs — Loading tips
    - UI/MapView.cs — Removed StairsDown references
    - UI/MinimapView.cs — Removed StairsDown references
    - UI/Helpers/MapEffects.cs — Removed StairsDown references

================================================================================
  DEAD FILE CLEANUP
================================================================================

  Removed 6 unused .cs files that were never referenced by active code:

  DELETED FILES:
    - Items/abstract_BaseItem.cs — Template leftover using wrong namespace (YourGame.Items)
    - Entities/PC.cs — Recruitable player character class, never instantiated
    - Items/Equipment/Equipment.cs — Superseded by EquipmentBase.cs (nothing inherits Equipment)
    - UI/Helpers/BoxDrawing.cs — Box-drawing character constants, never called
    - Items/Definitions/GatheredDefinitions.cs — Gathered item definitions, never referenced
    - Items/Materials/Gathered.cs — Gathered material class, never instantiated

  Build verified: 0 errors, 34 warnings (pre-existing nullable warnings).

================================================================================
  SAO LORE REFERENCE FILE
================================================================================

  Added SAO_Aincrad_Lore.txt to the project root — comprehensive lore reference
  covering canon (LN/anime) plus Hollow Fragment, Re:Hollow Fragment, Hollow
  Realization, Integral Factor, and Millennium Twilight. Excludes Alicization
  and GGO. Contains:
    - 38+ floor themes and locations
    - All known floor bosses (Floors 1-100 including Hollow Fragment F76-100)
    - 170+ sword skills across 12 weapon types, organized by source
    - Crystal items, notable weapons/armor/key items
    - Key characters (14 canon + 5 game-exclusive)
    - 7 guilds, 14 monster types, detailed game mechanics

================================================================================
  SAVE/LOAD SYSTEM (v1)
================================================================================

  Overview:
    JSON-based save/load system with checkpoint-at-floor-start model.
    Saves player state + progression; map is regenerated fresh on load.

  Save Location:
    %LocalAppData%/AincradTRPG/save.json

  How to Save:
    - F5 key during gameplay (manual quick save)
    - Auto-save triggers on floor ascend (AscendFloor)

  How to Load:
    - Title screen "Continue Game" / "Load Game" button
    - Checks SaveManager.SaveExists() to show appropriate label

  Hardcore Permadeath:
    - On player death with IsHardcore=true, save file is deleted

  Files Added:
    - Items/ItemRegistry.cs — Maps DefinitionId → factory method for item recreation
    - Systems/SaveData.cs — SaveData + ItemSaveData DTOs
    - Systems/SaveManager.cs — Save/Load/Delete with atomic writes (.tmp → rename)

  Files Modified:
    - Items/BaseItem.cs — Added DefinitionId property
    - All Items/Definitions/*.cs — Added DefinitionId to all 17 factory methods
    - Systems/TurnManager.cs — Added LoadFromSave() static factory + auto-save in AscendFloor()
    - Entities/Player.cs — Added LoadFromSave() static factory
    - Inventory/Core/Inventory.cs — Added ForceEquipForLoad() (no stat re-application)
    - UI/MapView.cs — Added F5/SaveRequested event
    - UI/GameScreen.cs — Added ShowFromSave(), F5 wiring, save-aware Show() with floor param
    - UI/TitleScreen.cs — Load button now checks SaveManager and calls ShowFromSave

  Item Serialization Strategy:
    - Definition items (DefinitionId set): saved as { id, durability, quantity }
    - Procedural items (DefinitionId null): saved with full property blob (FullItemJson)
    - ItemRegistry recreates definition items from their factory methods

  Equipment Stat Baking:
    - StatModifierCollection.ApplyTo() modifies BaseAttack/etc directly
    - On save: base stats already include equipment bonuses
    - On load: ForceEquipForLoad() places items without calling Equip()
    - This prevents double-applying stat bonuses

  Save Format:
    - SaveVersion = 1 (integer, for future migration support)
    - JSON with WriteIndented + WhenWritingNull ignore
    - Atomic file writes: save.json.tmp → File.Move → save.json

  Build verified: 0 errors, 34 warnings (pre-existing nullable warnings).

================================================================================
  END OF DOCUMENTATION
================================================================================
